# 2026-08-30

## 按 React Bits Pro Infinite Gallery 参考图调整无限画廊
- 文件：`F:\新建文件夹 (3)\zizao-gallery.html`
- 改动：
  - 背景改为纯黑舞台 + 轻微暗角，更贴近参考图。
  - 新增中心焦点大图（z=220，使用 CSS 变量尺寸），周围按 4 个深度圈层分布大量小图。
  - 卡片总数从 32 张增至 72 张（8 图 × 9 份），营造密集漂浮感。
  - 远景卡片更小、更暗、更模糊；近景更清晰明亮，强化景深。
  - 自动漂移速度放慢，整体更沉稳。
- 保留：点击任意卡片触发 Scroll Mask 六边形遮罩展开详情页。

## 新增手机端多点触控交互
- 双指捏合（pinch）缩放：围绕画面中心缩放整张画廊（viewScale 0.45–3.2，平滑过渡）。
- 双指拖动（pan）：双指中点位移平移相机，配合单指拖拽/自动漂移。
- 多指状态用 pointers Map 管理，单指=拖拽、双指=捏合+平移，松手后自然回退到单指。
- 强化景深：近处卡片不再额外缩小（scale=f×viewScale×近景微放大）、完全清晰明亮；远处卡片更大模糊（最高 7px）、更暗。
- 中心焦点大图锁定视口中心（每帧 x/y 同步相机），不参与漂移与 wrap，始终清晰居中。

## 重构：点击的卡片本身 FLIP 飞中心成为焦点大图
- 移除"独立居中焦点 DOM + 自动随机轮换焦点"方案。改为：72 张卡片全在无限阵列中，点击哪张 → 该卡片用 `focusAnimT` 缓动从原位置 FLIP 飞到画面中心放大成为焦点大图（zIndex 2000、清晰、带 glow），背景卡片虚化压暗。
- 文字信息用底部 `.detail-panel` 以 Scroll Mask（clip-path 从中心横向展开 + 上滑淡入）揭示拼音/六书/释义。
- 双指捏合缩放、双指平移、单指拖拽漫游、无限循环 wrap 全部保留。
- 关闭：焦点卡 `exiting=true` → `focusAnimT` 缓动回 0 飞回阵列，面板收起，背景恢复。
- 图库固定 8 张图，卡片随机分布 + 无限滚动。

## 修正：焦点大图改回流动版
- 移除“焦点锁定中心”分支，恢复上一版行为：焦点大图（安心）参与自动漂移与循环 wrap，会随画廊流动、在不同情绪图之间切换为视觉焦点。
- 双指捏合缩放（viewScale 全局）、双指拖动平移（pan）仍作用于整张画廊，因此焦点大图同样随双指放大缩小、拖动。
- 景深强化（近清晰微放大、远模糊）保留。

## 沉浸式东方 3D Infinite Gallery（cinematic 重写）
- 文件：`F:\新建文件夹 (3)\zizao-gallery.html`（整文件重写）。
- 视觉：纯黑无限空间；72 张卡片作为漂浮节点，四层 Z-depth（近/中近/中远/远）错落分布，非网格/非瀑布流，大量负空间。
- 交互新增/强化：
  - 拖拽/滑动漫游 X/Y 双向无限循环；松手惯性 + 缓慢减速（INERTIA_DECAY 0.93）。
  - 鼠标移动细微 camera parallax（按深度加权，近景偏移更多）。
  - Hover（桌面）：轻微放大 + 提亮 + 浮现汉字标签（触屏默认显示标签）。
  - 点击：卡片沿 Z 轴向用户靠近（z 从原值插值到 FOCUS_NEAR=540）并平滑移到中心放大，周围卡片保留为景深背景（轻虚化/压暗而非强遮挡）。
  - 双指捏合缩放 + 双指平移保留。
- 有机感：每张卡片正弦 bob + 轻微旋转抖动；空闲时镜头极缓自动漂移。
- 开场：黑屏逐句浮现「看见境→感受意→游字→触字→入字」后淡出进入画廊（可点“进入”跳过）。
- 已通过 node --check 语法校验；本地 http://localhost:8000 预览正常（HTTP 200）。

## 入字页 zizao-intro.html + 修复跳转
- 新增 `F:/新建文件夹 (3)\zizao-intro.html`（玻璃光标 + 书写「字」字识别跳转），背景用 `kaichangbeijingtu.jpg`（已存入项目目录）。
- 修复「跳转不了」：原识别门槛过高（avg>0.68 几乎不触发）且跳转依赖跨文件相对路径。
- 改为：几何兜底识别（笔画≥3、墨水长度>字符框0.5倍、覆盖中央区域>15% 即成功）+ 模板相似度兜底；新增右上「入境」兜底按钮（识别失败2次后出现，也可随时直达）；跳转用 `new URL('zizao-gallery.html', location.href)` + fallback 提示。
- 关键提示：present_files 的 static-html 临时预览沙盒不含 zizao-gallery.html，跨文件跳转会 404；必须在 `http://localhost:8000/zizao-intro.html` 或浏览器直接打开 `F:/新建文件夹 (3)\zizao-intro.html`（同目录有 gallery）才能成功跳转。已 node --check 通过、本地 HTTP 200。

## 合并为单文件 zizao.html（彻底解决「跳转不了」）
- 新增 `F:\新建文件夹 (3)\zizao.html`：把入字页（opening gate）与 3D 画廊**合并进同一文档**，消除跨文件 `window.location` 跳转依赖——这是「跳转不了」的根因。
- 打开即显示 kaichangbeijingtu 背景 + 玻璃光标书写「字」；成功（或点「入境」）后 `dismissOpening()` 仅做同页淡出并调用 `revealGallery()` 播放 5 境序列，再显露底层 3D 画廊。整条链路无页面跳转。
- 画廊的 5 境开场序列改为由入字成功后触发（initGallery 不再直接调 startIntro）。
- 8 张画廊 PNG 各 6–7MB（共 52MB），无法 base64 内联；故仍依赖同目录图片文件。图片加 `onerror` 兜底：加载失败则卡片显示汉字标签渐变块，预览沙盒缺图也不崩。
- 已 node --check 通过、本地 http://localhost:8000/zizao.html HTTP 200。推荐入口：用 localhost 或浏览器直接打开 `F:\新建文件夹 (3)\zizao.html`。

## zizao.html 三大增强（开场/选境/造字）
- 开场：背景 `kaichangbeijingtu.jpg` 加 `obgDrift` 漂移 + `.oriver` 流动光纹（repeating-linear-gradient 平移）+ SVG `feTurbulence` 焦散（金色河流流动感）。新增星光层：全局 `#starCanvas`（z1，画廊/选境背景）与开场 `#openingStars`（z2，河流之上光标之下），均 twinkle + 缓慢 drift + 偶发流光 streak。
- 写「字」识别更严但仍支持连笔：改用归一化模板 `templateMerged`（一笔连通路径）做 `strokeSimilarity`，cursive（连笔）与分笔都命中同一 merged 形状；新增结构校验（上下区覆盖、长宽比 0.5–1.9、墨长>0.85*size、覆盖率>0.30），阈值 sim>0.66。比旧版（geomOk 宽松或 sim>0.6）更严。仍保留右上「入境」兜底。
- 五境序列 → 情绪场景选择屏 `.mood-select`：文案「情绪，比语言更早抵达 / 为没有名字的情绪造字」，8 张情绪 tile（名+诗意短句 moodLines）错落浮入、悬停辉光（t-glow），选后揭示画廊并聚焦该情绪卡片；含「随缘漫游」。
- 卡片详情新增「开始造字之旅 →」按钮（#createBtn）→ 打开 `.create-journey` overlay：以所选情绪为种子，命名无名之感 + 书空 canvas（复用玻璃光标）+ 「落成此字」印章动画（sealStamp，含书空草图 dataURL 或首字）+ 返回画廊。
- 已 node --check 通过、本地 HTTP 200。重要：预览沙盒仍缺 8 张 png（仅单文件注入），画廊卡片会显示汉字标签占位；完整带图请用 http://localhost:8000/zizao.html 或双击 F 盘文件。

## 撤销情绪场景选择 → 改回 6 步开场叙事
- 删除 `.mood-select` 屏（CSS/HTML/JS 全删，含 buildMoodGrid/showMoodSelect/pickMood/msRandom 及 boot 调用）。
- 写「字」成功后不再 showMoodSelect，改为 `playNarrative()` 播放 6 步叙事：
  01 黑屏→中央偏下小暖灯（呼吸 2.8s，CSS lampBreathe）+ 案几 + 篆书残字背景 0.13 opacity 缓现；
  02 墨迹显字「你打开手机，想写下一句话」→「轻触屏幕」→ 触屏 ripple + 文字 tremble；
  03 「却发现有一种感觉」→「没有任何一个字能够准确表达。」→「准确表达」blur/drift 溶解 → 只剩「没有任何一个字」→ 全部消失归黑留灯；
  04 宋体+暖灰金 #B9A47A 渐现「有些情绪，曾经没有名字。」+ 四周旋转古文字残片（narv-frag, fragSpin）；
  05 「古人面对未知的事物时，创造了汉字。」→ 黑幕覆盖金色甲骨文 canvas，手指拖动 Glass Touch Trail 擦开（destination-out wipe）+ 金色粒子；
  06 甲骨文向中心聚拢成「字」再炸散 →「今天，也许你可以创造一个属于自己的字。」→「进入字造集」按钮 → revealGallery。
- 视觉关键词落地：夜/孤独/呼吸/墨/微光/古文字/手写/创造；9:16 深黑宣纸、暗金灯光、篆书残影、Glass Cursor/Touch Trail 复用。
- 已 node --check 通过、本地 HTTP 200。预览沙盒仍缺 8 张 png，画廊卡片显示汉字标签占位；完整效果用 http://localhost:8000/zizao.html。

## 开场 6 步叙事增强（酷炫/沉浸动效）
- 新增图层：`.narv-vignette`（暗角呼吸 z8）、`.narv-smoke`（灯上烟雾 z2）、`.narv-core`（核心光晕 z4）、`#narvFx` 粒子画布（z3）。
- 新增 FX 粒子系统：fxLoop 持续渲染 embers（暖色上浮火星，仅 step01 开 fxEmberOn）、ink 墨点（step03 溶解时从「准确表达」各字爆裂）、FxBurst（step02 触屏金色爆发）。
- 灯：`.narv-lamp.on` 叠加 `lampFlicker`（0.46s 火焰跳动），烟雾 `smokeRise` 上浮消散。
- step02：「你打开手机…」改为 `brush` 笔触逐字揭示（clip-path 左→右 + 模糊），触屏生成双层涟漪（.wide 111 倍）+ 金色 FxBurst。
- step03：溶解时 `spawnInk` 在每个「准确表达」字位爆出墨点 + 整行暖色墨雾。
- step04：`spawnFragments` 改为 `fragOrbit`（绕中心公转，随机半径/周期，残字直立旋转），并开启 `narvCore` 核心光晕脉动。
- step05：擦除时 `pushGlass` 沿途 `spawnSparks`（金色火花，gctx 上 screen 混合绘制）。
- step06：`oracleLoop` 的 gather 改为螺旋汇聚（ang+e*1.6π、半径收束），「字」带脉动发光（shadowBlur 正弦）；切 scatter2 时中心 `spawnSparks(46)` 爆裂；`drawSparks` 每帧绘制。
- 已 node --check 通过、本地 HTTP 200。预览沙盒仍缺 8 张 png（画廊卡片汉字标签占位）；开场动效不依赖图片，沙盒内可见。完整效果用 http://localhost:8000/zizao.html 或双击 F 盘文件。

## 撤销酷炫增强 → 回退到“干净 6 步叙事” + 三项微调
- 用户要求「撤销回上一版本」：移除上一轮（01-06 增强）的火星/烟雾/暗角/核心光晕/火焰跳动/笔触揭示/碎字公转/螺旋聚字/金色火花等 FX 系统（FX 粒子层 #narvFx、fxLoop/spawnInk/spawnFxBurst/spawnSparks/drawSparks、CSS .narv-vignette/.narv-core/.narv-smoke/.narv-fx、lampFlicker/brushIn/fragOrbit/rippleWide 等），恢复为早期「纯净 6 步叙事」：lampBreathe 呼吸、inkIn 逐字、fragSpin 自转、cubic gather、single ripple。
- 微调 1：写「字」识别放宽——`sim>0.66`→`0.5`，墨长阈值 `0.85*size`→`0.5*size`，长宽比 `0.5–1.9`→`0.32–2.4`，上下区覆盖 `0.12`→`0.06`，覆盖率 `0.30`→`0.18`，笔画数 `>=2`→`>=1`（允许连笔一笔成「字」）；接近阈值 `0.55`→`0.42`。
- 微调 2：「准确表达」四字溶解改为水墨冲刷感——`.dissolve`→`@keyframes inkWash`，每个字按随机方向 `--dx/--dy` 漂移+模糊放大+淡出（纯 CSS，无粒子）。
- 微调 3：「进入字造集」按钮改为镜面/液态玻璃风（圆角 pill、backdrop blur、白色内高光+暗金外晕、hover 斜向高光扫过）。
- 已 node --check 通过、本地 HTTP 200。预览沙盒缺 8 张 png，开场动效不依赖图片可见；完整带图用 http://localhost:8000/zizao.html 或双击 F 盘文件。

## 撤销镜面按钮 + 字卡「开始造字」→ 滚动遮罩 → 跳转下一个页面
- 撤销上一轮「进入字造集」镜面/液态玻璃按钮（backdrop blur + 斜向高光扫过），回退为干净描边金边按钮（圆角 3px、hover 暗金底+外晕）。
- 新增滚动遮罩转场（仿 React Bits Pro Scroll Mask，自实现无许可证）：`#scrollMask` 全屏黑幕，opacity 0→1（先变黑，0.7s），中心浮现所点情绪字（如「安心」）淡入；完全覆盖后 `window.location.href` 跳转。
- 字卡（点击画廊卡片后出现的 detail-panel）按钮文案「开始造字之旅」→「开始造字」；点击触发 `startScrollTransition(item)`：黑幕滚动遮罩 → 跳转 `zizaoji4_final_xuanxuan_audio_button/zizaoji4_final_xuanxuan_audio_button/index.html`（造字主程序，即用户的「下一个页面」）。
- 跳转目标确认：该 index.html 是完整多页 H5（情景引入→开场→六书实验室→能力值→造字工坊→构形分析→释义→字卡→资格证→海报→我的字造集），含玄玄语音助手（xuanxuan 组件）。跨文件跳转仅在 http:// 或 file:// 下有效，预览沙盒（仅注入单文件）无法验证，需本地服务器测试。
- 已 node --check 通过、本地 HTTP 200（zizao.html 与下一页 index.html 均可访问）。

## 修复桌面点击 + 删除开场/首页直接进入六书实验室 + 叙事页声音按钮
- **桌面端点击卡片无法进入字卡（根因修复）**：`onPointerDown` 的 `scene.setPointerCapture()` 把 `click` 事件重定向到 `scene`，卡片自身 click 监听桌面端永不触发。修复：移除 setPointerCapture；新增 `cardEls` Map 与 `downCardEl`，在 `onPointerUp`（指针数 0、dragDistance<=12、落在 `.card`）直接 `enterFocus`，桌面/触屏均可靠。
- **NEXT_PAGE 修正**：zizao.html 与 index.html 是兄弟文件（同在 `zizaoji4_final_xuanxuan_audio_button/` 下），`NEXT_PAGE` 由深层相对路径改为 `'index.html'`（原路径会 404）。
- **index.html 直接进六书实验室**：删除 `page-story`(首页) 与 `page-intro`(开场页)；main.js `init()` 改 `navigateTo('lab')`，兜底与 name 提交也指向 `lab`，`initFns` 移除 `initStory`/`initIntro`。首屏即「六书实验室」。
- **叙事页声音按钮**：`#narrative` 右上角 `#narvSound`（用 `assets/icons/sound-icon.svg`）控制 `assets/audio/bgm/home-guqin.mp3`（默认静音、点按开启、离开叙事暂停）；`stopPropagation` 防误触推进步骤；静音态显示斜杠。
- 验证：zizao.html 内联脚本与 main.js 均 `node --check` 通过；本地 8011 serving 子目录，zizao.html/index.html/sound-icon.svg/home-guqin.mp3/style.css/main.js 全部 200。
- 已知：8 张情绪 png 与 kaichangbeijingtu.jpg 当前工作区缺失，画廊/开场背景走 `img-missing` 汉字标签兜底（功能正常，仅无照片）。

## 修正画廊卡片与开场背景（图片均在顶层）+ 叙事页新增跳过按钮
- **架构纠正**：zizao.html 实际位于 `F:\新建文件夹 (3)\zizao.html`（与 8 张情绪 png、kaichangbeijingtu 同目录），并非此前以为的与 index.html 兄弟。声音/BGM 资源（`sound-icon.svg`、`home-guqin.mp3`）仅在子目录 `zizaoji4_final_xuanxuan_audio_button/.../assets/` 下。
- **补齐 qidai.png**：顶层原缺 `qidai.png`（期待），从子目录 `assets/images/qidai.png` 复制到顶层，使 8 张情绪图全部可用。
- **开场背景修复**：引用 `kaichangbeijingtu.jpg` 但文件实为 `kaichangbeijingtu.png`，改为 `.png`（文件已在顶层）。
- **资源路径修复**：声音图标 `assets/icons/sound-icon.svg` 与 BGM `assets/audio/bgm/home-guqin.mp3` 在顶层 zizao.html 下解析不到，改为指向子目录 `zizaoji4_final_xuanxuan_audio_button/zizaoji4_final_xuanxuan_audio_button/assets/...`。
- **NEXT_PAGE 修正**：原为 `'index.html'`（兄弟假设错误），改为子目录相对路径 `zizaoji4_final_xuanxuan_audio_button/zizaoji4_final_xuanxuan_audio_button/index.html`。
- **新增跳过按钮（6 步叙事右上角）**：`#narvSkip`（胶囊「跳过」，位于声音按钮左侧 top:18px right:74px），叙事开始加 `.show` 显示、revealGallery 时移除。点击 `revealGallery()` 直接进入画廊。新增 `skipRequested` 标志，使 `wait/waitFirstTouch/enableWipe/gatherWord` 在跳过时立即结束，避免卡在等待用户输入（enableWipe/waitFirstTouch 尤其会永久挂起）。
- 验证：内联脚本 `node --check` 通过；本地 8020 serving 顶层，zizao.html、8 张情绪 png、kaichangbeijingtu.png、子目录 index.html、sound-icon.svg、home-guqin.mp3 全部 200。

## 六书实验室及后续页面「高级化 + 自适应」整体优化（additive）
- **新增 `css/enhance.css`**（在 index.html 中于 `css/style.css?v=...` 之后引入，链接 `css/enhance.css?v=20260830-01`）。原则：**不改任何 DOM 结构与 main.js 逻辑，功能 100% 不变**，纯样式/动画增强层。
- **高级感**：补充设计令牌（ink/金/seal 渐变、玻璃拟态、金线描边、层次阴影）；六书实验室关卡升级为玻璃面板（金色顶饰+模糊背景）；能力值/字卡/资格证统一成高级卡片（玻璃+金线+纸纹底）；按钮改为墨黑/朱红渐变 + 悬浮上抬 + 微光扫过（shimmer）；标题用墨色渐变文字。
- **更好玩**：象形太阳 idle 漂浮 + 时间轴当前帧脉冲光环；会意拖拽件玻璃磁吸悬浮；指事红点脉冲光圈；关卡成功庆祝弹出（金色描边+印章旋转+徽章投影）；各页卡片入场 `enhanceRise` 上浮、进度点流动金线；按钮 hover 微光。
- **自适应**：手机端(≤360)细化间距字号、构件库 5 列；平板(min-width:600) 把 #app 由 500px 窄列升级为居中"展台"卡片（圆角 24px+深阴影+金线边框）并加桌面环境光(body::after 双角光晕)；桌面(min-width:900) 拓宽至 660px、构件库 10 列、字造集网格 3 列、海报放大；内部尺寸用 clamp()/flex/grid 自适应。
- 验证：enhance.css 大括号 178/178 平衡、注释闭合；main.js `node --check` 通过（未改动）；本地 8030 serving 子目录，index.html/css/enhance.css/css/style.css/assets/images/paper-bg.jpg/js/main.js 全部 200。

## 四项修改（声音按钮/边框光晕/移动端字卡跳转/纯 bgm）
- 仅改两文件：`F:\新建文件夹 (3)\zizao.html`（开场叙事页）与子目录 `js/main.js`（主程序音频引擎）。
- **1. 叙事页右上角两按钮缩小 + 声音控全局**：`#narvSound` 46→34px、`#narvSkip` 46→34px（right 74→54 避免重叠）；`narvSound` 点击除控制 `narvBgm`(home-guqin) 外，新增持久化 `window.__zizaojiMuted` + `localStorage['zizaoji_music_muted']`，主程序 main.js 的 bgm 与 sfx(`playSfx`) 均已被该标志 gate，故开场静音会延续到主程序（反之亦然）。
- **2. 边框光晕按钮（实现 React `BorderGlow` 等价）**：新增 `.border-glow` CSS（conic 彩虹 `#c084fc/#f472b6/#38bdf8` 边框 + 鼠标临近边缘的 radial 光晕 `rgba(40 80 80)`，mask 只显示 2px 边框环，`borderRadius:28`）+ `initBorderGlow(el,opts)`（edgeSensitivity/glowColor/glowRadius/glowIntensity/colors 入参）。应用于「开始造字之旅」两个按钮 `#cjBack`/`#cjCommit`（深色 `#120F17` 底、金/浅色字，hover 近边缘发光）。「进入字造集」加 `white-space:nowrap` + 移动端缩小，保持单行。
- **3. 修复手机端点击字卡不跳转**：原 `dragDistance<=12` 阈值在触屏易被手指位移突破；改为记录 `downX/downY`，`onPointerUp` 用按下/抬起位移 `moved<=24` 判定轻点。`(hover:hover)` 设备仍走「聚焦→开始造字」；**触屏(`hover:none`)轻点字卡直接 `startScrollTransition` 跳转 index.html**（彻底修复「点击字卡跳转不了页面」）。
- **4. 背景音乐只用 bgm 目录**：main.js `bgmMeaning` 由 `sfx/meaning-story.mp3` 改为 `bgm/poster-theme.mp3`，现四类 bgm（home-guqin/collection/poster-theme）全部来自 `assets/audio/bgm`；`element/`、`sfx/` 仅作音效，符合「只应用 bgm 做背景音乐」。
- 验证：zizao.html 内联脚本与 main.js 均 `node --check` 通过。预览需本地 http 或 file://（跨文件跳转到子目录 index.html、缺 8 张 png 走汉字标签兜底）。

## 新一轮修改（声音默认开 + 移动端字卡全流程）
- **1. 声音按钮默认「开启」+ 首页 BGM 解锁**：`#narvSound` 移除 `muted` 类（默认显示开启，无斜杠）；`window.__zizaojiMuted=false` 且 `localStorage['zizaoji_music_muted']='0'`。新增 `tryStartHomeBgm`：首次 `pointerdown/touchstart/keydown` 手势即 `narvBgm.play()`（绕过浏览器自动播放策略）。**关键修复「跳转后没音乐」根因**：上一版默认 muted → main.js 读到 muted=true 而全程静音；改为默认开后，main.js `init()→navigateTo('lab')→pageBgm('lab')→startBgm` 在跳转后首页立即续播 bgmCollection，背景音乐不中断。
- **2. 字卡按钮改名**：`#createBtn` 文案「开始造字」→「开始造字之旅」（含箭头）。
- **3. 修复手机端「点卡片未显示字卡」**：上一轮为修跳转把触屏分支改成直接 `startScrollTransition`（跳过字卡），用户要求走完整流程。改为触屏也调用 `enterFocus(obj)`（与桌面一致：点卡片→字卡浮现→点「开始造字之旅」→滚动遮罩→下一个页面）。同时把 `.detail-overlay` z-index 由 100 提到 4000（高于聚焦卡片 3000），确保字卡面板与「开始造字之旅」按钮在手机端始终可点。
- 验证：zizao.html 内联脚本 `node --check` 通过。

## 再修：手机端字卡全流程 + 跳转后 BGM 续播
- **1. 手机端「点卡片→字卡浮现→开始造字之旅→滚动遮罩→下一个页面」仍不通（根因）**：上一轮的轻点判定用「按下↔抬起 clientX 位移 moved<=24」，触屏手指漂移 / `pointercancel` 携带脏坐标易超阈，且只走 pointer 事件（部分浏览器吞掉合成 click）。改为：(a) 主判据用累积 `dragDistance<=30`（与坐标无关，pointerup/pointercancel 均可靠触发）；(b) 新增 `didDrag` 标志（拖拽距离>12 即 true），拖拽时不打开字卡；(c) `scene` 额外挂 `click` 兜底监听 `closest('.card')` → `tryFocusCard`；(d) `tryFocusCard` 加 350ms 防抖，避免 pointerup 与合成 click 双触发。桌面/触屏统一走 `enterFocus` 完整流程。
- **2. 跳转后 BGM 不中断**：main.js `init()` 原仅 `navigateTo('lab')→pageBgm('lab')`，但新页面加载时 `unlocked=false`，`pageBgm` 只准备 bgm 不播放，须等首次手势 `ensureBgmRunning→unlock` 才出声（即跳转后静音直到用户再点）。在 `navigateTo('lab')` 后新增 `AudioEngine.pageBgm('lab'); AudioEngine.unlock();`：依赖同源 Media Engagement 在加载即续播 bgmCollection；若被自动播放策略拦截，首次交互由 `ensureBgmRunning` 兜底恢复，杜绝跳转后静音。所有 bgm 仍 100% 来自 `assets/audio/bgm`（home-guqin/collection/poster-theme）。
- 验证：zizao.html 内联脚本与 main.js 均 `node --check` 通过。

## 胶片画廊（Reel Gallery 原生等效）替换情绪长廊
- 用户要求采用 React Bits Pro `reel-gallery` 效果，但 Pro 需授权 + React 构建，与单文件 H5 约定冲突 → 用户确认**原生等效实现**，应用于多字卡画廊页（情绪长廊）。
- 实现（均改 `zizao.html`）：新增 `.reel-gallery`/`.reel`/`.reel-frame` CSS（胶片齿孔上下打孔条 + 倾斜卷轴 + 帧 hover 提亮上浮）；新增 `#reelGallery>#reelTrack` 容器；`initReelGallery()` 生成 4 条卷轴（speed 正负交替、rot 倾斜、每轴 14 帧复用 8 情绪）；`reelGallery` 纵向滚动时 `onReelScroll` 按 `scrollTop*speed` 横向平移各卷轴（glide）。点击帧 → `enterZika(item)` 弹小字卡（复用 `#detailOverlay`，设 `currentZikaItem`）。`currentZikaItem` 取代 `focusedCard` 作为字卡数据源（createBtn/Esc/exitFocus 同步）；`revealGallery` 切 `body.reel-mode`（隐藏 3D `#scene`、显示 `#reelGallery`、提示文案改为「滚动浏览胶片·点击字卡入字」）；BOOT 由 `initGallery()` 改 `initReelGallery()`（3D 漂浮画廊代码保留但不再初始化）。BGM 续播逻辑不变。
- 验证：zizao.html 内联脚本 `node --check` 通过。

## 修复「情绪长廊没有背景音乐」
- 根因：`revealGallery()` 内 `if (narvBgm) narvBgm.pause();` 在进入画廊时把 home-guqin 暂停了 → 画廊无声。
- 修法（`zizao.html`）：`revealGallery` 改为「若未静音且已暂停则 `narvBgm.play()`」，进入情绪长廊后背景音乐继续播放不中断；未开始时也在此解锁播放。配合 main.js 已统一的 home-guqin(全部 bgm track→home-guqin + sessionStorage 进度续播)，满足「整作统一 home-guqin + 跳转后不中断」。
- 验证：zizao.html 内联脚本 `node --check` 通过。

## 再修（手机/电脑端点卡片→字卡 + 全作统一 home-guqin 且跳转不中断）
- **1. 点卡片字卡不显示（最终根因）**：`enterFocus` 给 `#detailOverlay` 加 `.active`（z-index 4000、全屏、pointer-events:auto）后，同一手势的**合成 click 被命中测试打到遮罩自身**，触发 `detailOverlay` 的 click-to-close 监听立即 `exitFocus()`，字卡在同 tick 内开即关 → 看不到。修复：在 `detailOverlay` click 监听加 450ms 守卫 `if (performance.now() - lastFocusTs < 450) return;`（复用 tryFocusCard 的 lastFocusTs）。桌面/手机统一走 `enterFocus` 完整流程（点卡片→字卡→开始造字之旅→滚动遮罩→跳转）。
- **2. 整个作品统一 home-guqin 且跳转不中断**：main.js `tracks` 的 `bgmCollection`/`bgmMeaning`/`bgmPoster` 全部改为 `bgm/home-guqin.mp3`；`bgmByPage` 所有页 → `bgmHome`；`setMeaningStyle` 固定 `key='bgmHome'`（单 Audio 对象，切页不重启）。跳转前在 `startScrollTransition` 记录 `sessionStorage['zizaoji_bgm_pos'] = narvBgm.currentTime`；main.js `init()` 跳转后续播时把 `getAudio('bgmHome').currentTime` 设为该进度（loadedmetadata 后应用），实现「同一首 home-guqin 不中断地接着播」。
- 验证：zizao.html 内联脚本与 main.js 均 `node --check` 通过。
