# 2026-09-01

## 调整玄玄位置 & 详情页隐藏玄玄（10:21）
- **需求**：玄玄位置下移一点与气泡对齐；参考图所示的情绪字卡详情页删除玄玄。
- **实现**：
  - `zizao.html`：在 `enterFocus`（打开字卡详情）时 `window.XuanXuan._el.style.display = 'none'`；在 `exitFocus`（关闭详情）时恢复 `display = 'block'`（不重新触发入场台词）。
  - `xuanxuan.css`：`.xuan-zizao` 保持 `bottom: 64px`（桌面）/ `52px`（移动），注释更新为“位置与气泡自然对齐”。
- **验证**：grep 确认 hide/show 代码已注入正确位置，xuan-zizao 定位值正确。

## 玄玄左移下移、气泡固定（10:33）
- **需求**：玄玄角色左移并下移，气泡保持在屏幕原位置不动。
- **实现**：`xuanxuan.css` 把 `.xuan-zizao .xuan-dialog` 改为 `position:fixed`（桌面 `right:96px; bottom:128px`，移动 `right:60px; bottom:96px`），脱离角色独立固定；角色 `.xuan-zizao` 移到 `right:128px; bottom:16px`（桌面）/ `right:84px; bottom:14px`（移动）。新增 `xuanBubbleZizao` 关键帧覆盖气泡入场动画（去掉全局 `translateX(-50%)`），箭头 `::after` 改为指向角色（底部）。
- **注意**：气泡 fixed 后不受角色移动影响，符合「气泡位置不变」。

## 多页面玄玄位置与海报页排版（10:42）
- **需求**：
  1. 六书实验室页（图1）：玄玄与气泡整体左移并下移，避免遮挡中间圆形与文字。
  2. 字卡页（图2）：玄玄与气泡整体下移，避免遮挡「查看造字资格」按钮与字卡。
  3. 海报页（图3）：三个按钮大小/字号/圆角一致，海报整体排版更舒展。
- **实现**：
  - `xuanxuan.css`：
    - `.xuan-lab` 桌面 `right:90px; bottom:8px`（原 `right:12px; bottom:16px`），移动 `right:56px; bottom:6px`。
    - `.xuan-charcard` 桌面 `right:18px; bottom:60px`（原 `right:12px; bottom:130px`），移动 `right:10px; bottom:48px`。
  - `index.html` + `css/style.css` + `css/enhance.css`：
    - 海报页 HTML 把三个按钮统一放进 `.poster-actions`：第一行「我的字造集」+「保存海报」并排，第二行「重新造一个字」居中。
    - 三个按钮统一 `padding: 10px 18px; font-size:13px; border-radius:22px`。
    - 海报容器 `max-width: clamp(280px,78vw,340px)` + `max-height:56vh`，`margin-bottom` 增大到 `28px`，整体更舒展。
    - 响应式 `max-width` 同步调小到 `360px`（768px+）和 `400px`（900px+）。
- **验证**：`index.html` 海报页 HTML 结构正确；`curl` 预览服务 `localhost:8084/index.html` 返回 200。

## 撤销多页面玄玄位置与海报页排版（11:12）
- **用户要求**：撤销上一步（10:42）对 xuanxuan.css 玄玄定位 + 海报页排版的全部修改，恢复原状。
- **已恢复**：
  - `xuanxuan.css`：`.xuan-lab` 桌面 `right:12px; bottom:16px`、移动 `right:8px; bottom:12px`；`.xuan-charcard` 桌面 `right:12px; bottom:130px`、移动 `right:8px; bottom:100px`（均为原值）。
  - `index.html`：海报页 HTML 恢复为「我的字造集」+「保存海报」并排一行 + 下方独立整宽「重新造一个字」。
  - `css/style.css` P09：海报容器 `max-width:320px`、无 `max-height`、按钮恢复原缩小样式（`padding:8px 14px` 等）。
  - `css/enhance.css`：海报容器 `clamp(300px,84vw,380px)`、媒体查询 `max-width` 恢复 `400px`(600px+) / `440px`(900px+)。
- **验证**：grep 确认无 `poster-actions-row` 残留引用；各文件数值已回到原状。

## 再次撤销（恢复玄玄+海报页修改）（11:15）
- 用户随后说「撤销」= 撤销上一步的撤销，即把 10:42 的玄玄定位 + 海报页排版改动**重新应用**回来。
- 已恢复全部 10:42 状态：`.xuan-lab`(right:90/bottom:8; 移动 right:56/bottom:6)、`.xuan-charcard`(right:18/bottom:60; 移动 right:10/bottom:48)；海报页 `.poster-actions-row` 结构、统一按钮尺寸、`clamp(280px,78vw,340px)`、`max-height:56vh`、响应式 `360px/400px`。
- 结论：当前文件状态 = 10:42 修改版（非原始版）。

## 撤销（恢复原始版，11:20）
- 用户说「撤销上一步」= 把 11:15 重新应用的 10:42 修改**再次撤销**，文件回到最初原始状态。
- 已重新回退：`xuanxuan.css` 玄玄定位恢复原始（`.xuan-lab` right:12/bottom:16 等）；海报页结构/样式/响应式全部回到原状（clamp(300px,84vw,380px)、320px、400px/440px）。
- grep 确认无 `poster-actions-row` 残留。当前文件 = 原始版（10:42 修改已被完全撤销）。

## 再次撤销（回到修改版，11:23）
- 用户说「撤销，回到 10:35 的版本」= 撤销上一步的撤销，重新应用 10:42 修改版（玄玄定位左移下移 + 海报页按钮统一/排版舒展）。
- 已重新应用：`.xuan-lab`(right:90/bottom:8; 移动 right:56/bottom:6)、`.xuan-charcard`(right:18/bottom:60; 移动 right:10/bottom:48)；海报页 `.poster-actions-row` 结构、统一按钮尺寸、`clamp(280px,78vw,340px)`、`max-height:56vh`、响应式 `360px/400px`。
- 重启预览服务：index 8084、zizao 8085 均 200。
- 结论：当前文件 = 10:42 修改版。

## 字卡页玄玄整体下移（11:54）
- 页面：`#page-charcard`（含「查看造字资格」按钮）。
- 需求：右下角玄玄卡通形象 + 上方「我把它记下来了！」气泡整体垂直下移一段距离；下移后气泡仍绑定角色（对话气泡是 `#xuanxuan` 子元素，`position:absolute;bottom:106%`，移动容器即一起下移）；玄玄靠右下但不贴边。
- 修改 `xuanxuan.css` `.xuan-charcard`：桌面 `right:18px;bottom:60px` → `right:26px;bottom:30px`；移动 `right:10px;bottom:48px` → `right:16px;bottom:24px`。未动对话框 CSS（靠子元素绑定保持位置不变）。

## 转盘页面（P05 六书构形分析）玄玄左下移（11:38）
- 用户要求修改 page-analysis 转盘页玄玄+气泡：整体向左下平移，避开中央圆环/中心字卡/底部「模板构形中……」；玄玄靠右下不贴边，气泡在玄玄左上方不遮挡主体。
- 修改 `xuanxuan.css`：拆分 `.xuan-analysis` 与 `.xuan-meaning`（原共用 right:12/bottom:110）。
  - `.xuan-analysis` 桌面 `right:22px; bottom:60px`（原 right:12/bottom:110，整体下左平移，仍靠右下不贴边）；移动 `right:14px; bottom:48px`。
  - 新增 `.xuan-analysis .xuan-dialog{ left:auto; right:0; transform:none; }`：气泡改到玄玄左上方（从右上沿向左上展开，远离居中主体）。
  - `.xuan-meaning` 保持原值 right:12/bottom:110（移动 right:8/bottom:80），未被误改。

## 字卡页玄玄整体左移（11:58）
- 页面：`#page-charcard`（含「查看造字资格」按钮）。
- 需求：右下角玄玄卡通形象 + 上方「我把它记下来了！」气泡整体向左移一段距离（气泡绑定角色，一起左移）；靠右下不贴边。
- 修改 `xuanxuan.css` `.xuan-charcard`：桌面 `right:26px;bottom:30px` → `right:64px;bottom:30px`（左移约 38px）；移动 `right:16px;bottom:24px` → `right:40px;bottom:24px`（左移约 24px）。未改 bottom 与对话框 CSS。
- 预览服务 8084(index)/8085(zizao) 仍 200，刷新即生效。

## 字造集页（P10）玄玄左移上移（14:42）
- 页面：`#page-collection`（含「继续造一个字」全宽按钮 #btn-collection-new + 免责声明）。
- 需求：玄玄+气泡整体向左移，远离右边缘保持安全边距；不遮挡按钮/文字；不贴边/裁切/重叠。
- 修改 `xuanxuan.css` `.xuan-collection`：桌面 `right:12px;bottom:14px` → `right:60px;bottom:76px`（左移48px + 上移避开底部按钮/免责声明）；移动 `right:8px;bottom:10px` → `right:36px;bottom:64px`。气泡随角色绑定一起移动，未改对话框 CSS。
- 验证：grep 确认 collection 页经 `XuanXuan.show('collection',...)` 应用 `.xuan-collection` 类；气泡居中宽≤160(桌面)/130(移动)，右移后左边界距右边缘约 180/129px，不裁切。

## 字造集页玄玄再左移下移（14:49）
- 页面：`#page-collection`（含「继续造一个字」全宽按钮 + 免责声明）。
- 需求：在 14:42 基础上「再往左和往下移」。
- 修改 `xuanxuan.css` `.xuan-collection`：桌面 `right:60px;bottom:76px` → `right:92px;bottom:68px`（再左移32px + 下移8px，仍高于按钮顶部、不压按钮/免责声明）；移动 `right:36px;bottom:64px` → `right:56px;bottom:56px`（再左移20px + 下移8px）。
- 说明：因底部按钮为全宽，玄玄 `bottom` 不能过小，否则会压到按钮/免责声明；故「往下」仅移到刚好不压的安全临界位。若需彻底贴底需接受压住按钮/免责声明。

## 字造集页玄玄修正：抬到按钮正上方（14:53）
- 页面：`#page-collection`（全宽「继续造一个字」按钮 + 免责声明）。
- 需求：「下移，不要遮挡继续造一个字按钮」。
- 测量布局：`.page` height=100%（视口高），grid `flex:1` 把按钮+免责声明顶到页面最底；按钮顶距视口底 ≈ 92–107px（disclaimer 高~31 + btn-primary 高~46 + padding 15）。玄玄角色高约 80px。
- 结论：此前 `bottom:68/56` **实际压在按钮上**（68<92）。要不压按钮，玄玄底必须 ≥ 按钮顶，即只能停在按钮**正上方**；「往下」会更深压住按钮，与约束冲突。
- 修改 `xuanxuan.css` `.xuan-collection`：桌面 `right:92px;bottom:68px` → `bottom:110px`（上抬到按钮上沿，不压）；移动 `right:56px;bottom:56px` → `bottom:100px`。`right` 维持左移后 92/56 不变。
- 经验：玄玄高度有限 + 底部全宽按钮时，"避免压按钮"等价于把玄玄放在按钮正上方（bottom≥按钮顶距底），而非下方。

## 字造集页玄玄再下移（15:00）
- 需求：在「抬到按钮正上方」(bottom:110/100) 基础上继续「下移」。
- 复测底部结构（距视口底，自下而上）：页面 padding 15 → 按钮高~46（顶在 61px）→ 间距 15 → 免责声明 76–107px。
- 结论：不压按钮的最低安全位 = 玄玄底边落在按钮顶(61px)之上；`bottom:64` 会压住免责声明小字、但刚好不碰按钮——即用户要的「最底角下移」效果。
- 修改 `xuanxuan.css` `.xuan-collection`：桌面 `bottom:110px → 64px`；移动 `bottom:100px → 56px`。`right` 维持 92/56 左移值不变。
- 说明：此位会遮挡免责声明那行小字，但不遮挡「继续造一个字」按钮；若需再低需接受压按钮，届时需用户确认。

## 字造集页玄玄落到底角（15:03）
- 需求：在 15:00 的 `bottom:64/56`（已压免责声明、不压按钮）基础上「再下移」——即用户接受压住按钮。
- 修改 `xuanxuan.css` `.xuan-collection`：桌面 `bottom:64px → 24px`（落屏幕右下角，仅留页面内边距以上的安全边，不贴屏幕边/不裁切）；移动 `bottom:56px → 18px`。`right` 维持 92/56 不变。
- 现状：玄玄现重叠在「继续造一个字」按钮右上角 + 免责声明之上。若用户后续觉得遮按钮过多，可再上抬或挪到左下/纯空白区。

## 字造集页点击字卡后隐藏玄玄（15:07）
- 需求：字造集页（P10）点击字卡弹出作品详情（#detail-modal）后，隐藏玄玄；关闭详情恢复。
- 实现（`js/main.js`）：`showCharDetail()` 加 `.show` 后调用 `XuanXuan.hide()`；关闭且停留在 collection 页的两处（`#detail-close`、`#detail-delete`）移除 `.show` 后 `XuanXuan._el.style.display='block'` 恢复（不重触发入场动画/台词）。
- `#detail-regen` 跳转海报页，玄玄由 poster 页 `XuanXuan.show('poster')` 自动隐藏，无需额外恢复。详情弹窗无遮罩点击关闭，仅上述按钮触发关闭。
- 校验：`node --check js/main.js` 通过；8084 仍 200。

## 海报页布局三项调整（15:22）
- 需求：海报页（P09 #page-poster）①三个按钮+免责声明往下调；②海报比例不压缩；③顶部切换背景图按钮(#poster-bg-switch)往上调。
- 根因：原 `.poster-container` 用 `width:100% + max-height:56vh + aspect-ratio`，矮屏下 max-height 压高度但宽度仍 100% → 容器比例被压扁、画布拉伸（即「压缩」）；整页 `justify-content:center` 又把 bg-switch 顶到中部。
- 修改：
  - `css/style.css` `#page-poster`：`justify-content:center → flex-start`；`padding:18px → 12px 18px 14px`（bg-switch 回到顶部）。
  - `css/style.css` / `css/enhance.css` `.poster-container`：去掉 `max-height`，改为 `height:calc(100vh - 230px); max-height:540px; aspect-ratio:9/16; width:auto; max-width:92vw;`——以高度定尺寸锁定 9:16，比例恒定不压缩，且预留 230px 给 bg-switch/按钮/免责声明，不溢出。
  - `css/style.css` `.poster-actions`：加 `margin-top:auto`，把三个按钮+免责声明整体推到页面底部（与 bg-switch 上移形成上下拉开）。
- 校验：8084 仍 200；900px 断点给 poster-container 的 max-width:400 不触发（推导宽≤303）。

## 造字结果卡片页升级为「私人造字档案 / 字笺」（15:35）
- 需求：P06.5 古籍字卡页（#page-charcard）升级为具东方设计感的「私人造字档案 / 字笺」（私人造字笺纸）。
- HTML（`index.html`）：保留原 ID（charcard-char/components/style/meaning/footer）以兼容 JS；重构为笺纸：头部「私人造字档案」+ 档案号(#charcard-no「字笺·第〇〇壹号」)；body 左 glyph-col(大字+构件) 右 meaning-col(竖排「释文」)；meta 行(构形#charcard-structure/风格#charcard-style-wrap/造字人格#charcard-personality)；落款(#charcard-footer 造字者+日期)。容器加 `zhijian` 类。
- CSS（`css/style.css`）：`.charcard.zhijian` 宣纸底+双线描边(外墨线+内描金::after)+角印；`.charcard-frame::before` 左侧装帧朱线(竹简/线装书意象)；`.charcard-meaning` 用 `writing-mode:vertical-rl` 竖排释文（长文>16字自动加 `is-long` 回退横排防裁切）；`.charcard-meta` 金线分隔。古风极简、克制用色（朱/金≤10%）。
- JS（`js/main.js` `renderCharCard`）：新增 `toArchivalNum`(序号→三位于大写汉字编号)、`structureFallbackName`(结构兜底)、`formatArchiveDate`(YYYY.MM.DD 落款)；编号取 `AppState.collection.length+1`，构形取 `AppState.presets.structures[structure].name+'结构'`，造字人格取 `char.personality.name`，风格无选中则隐藏整项。
- 校验：`node --check js/main.js` 通过；新服务 8086 返回含字笺结构的 HTML（CSS/JS 新逻辑均已生效）。预览：http://localhost:8086/index.html（进六书实验室→…→释义页→赋予意义→字笺页）。

## 字笺再升级：生成汉字为绝对视觉中心 + 宣纸飞白质感（15:46）
- 需求：在上一版「字笺」基础上，让中央生成汉字成为绝对视觉中心，且不再是普通白卡——要像精致宣纸字笺：极淡纸张纹理、局部金线、自然墨色浓淡、毛笔飞白。
- 根因：上一版字卡为左右分栏（大字在左、竖排释文在右），字并非视觉中心；且 `--font-banshu` 此前从未定义，毛笔字实际回退到默认字体。
- 修改（`css/style.css`）：
  - `:root` 补定义 `--font-banshu: "STXingkai","华文行楷","STKaiti","KaiTi","Kaiti SC","Songti SC",serif`（行楷/毛笔体）。
  - `.charcard.zhijian` 背景由玻璃白改为宣纸底色(`linear-gradient` 暖米 + #f3ead8)；新增 `::before` 叠 paper-bg.jpg + SVG feTurbulence 噪声，opacity .22 / multiply → 极淡纸张纹理（非白卡）。
  - 删 `.charcard-body/.charcard-glyph-col/.charcard-meaning-col`，新增 `.charcard-stage`：字心舞台 flex 居中；`::before` 柔和墨晕(radial-gradient 浓淡)，`::after` 细金圈(局部金线)。
  - `.charcard-ink`(#charcard-char 大字)：font-size 130px、`background:linear-gradient(150deg,...)` 斜向墨色浓淡 + `background-clip:text`；`mask-image` 用 SVG feTurbulence→feColorMatrix(1-A^3) 噪声镂空 → 毛笔飞白（白丝）；亦作用于图片态(.charcard-glyph-img 复用该 mask)。
  - `.charcard-meaning` 改居中横排短注（原竖排），下方加 `.charcard-meaning-label`「释文」居中朱字小标；`is-long` 长文回退横排。
- HTML（`index.html`）：`#page-charcard` 内 `.charcard-body` 整块替换为 `.charcard-stage`(大字#charcard-char + 构件#charcard-components) + 居中「释文」标签 + `.charcard-meaning`(#charcard-meaning)。其余 ID 不变。
- JS（`js/main.js` `renderCharCard`）：图片态由内联 100px 改为 `<img class="charcard-glyph-img">`（统一尺寸 + 复用飞白 mask）。
- 校验：`node --check js/main.js` 通过；8086 返回新结构(CSS 含 charcard-stage/charcard-ink/font-banshu/feibai mask 均已生效)。预览同 8086。

## 撤销字笺「居中舞台 + 飞白」升级，回到 15:35 侧栏字笺版（15:57）
- 用户要求「撤销回15:29版本」= 撤销上一条(15:46)字笺再升级，回到 15:35 的「私人造字档案/字笺」侧栏版。
- 回退内容（`css/style.css` + `index.html`，JS 不动、ID 全部保留故不破）：
  - `:root` 删除 `--font-banshu` 定义（恢复 15:35 未定义状态，大字回退默认字体）。
  - `.charcard.zhijian` 由宣纸底色(#f3ead8+纹理)改回玻璃白(`var(--glass-strong)`)；删除 `::before` 纸张纹理与 `::after` 内描金双框。
  - 删除 `.charcard-frame` 及左侧装帧朱线 `::before`；删除 `.charcard-stage`/`::before` 墨晕/`::after` 金圈/`.charcard-ink`(130px 飞白大字)。
  - 恢复 `.charcard-body`(flex 左 glyph-col + 右 meaning-col) + `.charcard-glyph-col`/`.charcard-char`(60px)/`.charcard-meaning`(`writing-mode:vertical-rl` 竖排释文，`is-long` 长文回退横排)/`.charcard-meaning-label`(竖排朱标)。保留 `.charcard-glyph-img`(96px) 供图片态复用。
  - HTML：`#page-charcard` 内 `.charcard-frame`>.charcard-stage 改回 `.charcard-head` + `.charcard-body`(glyph-col+meaning-col) + `.charcard-meta` + `.charcard-sign`，ID 不变。
- 校验：grep 确认无 charcard-stage/charcard-ink/charcard-frame 残留；8086 返回含 charcard-body、CSS 无 charcard-stage。预览同 8086。

## 撤销「私人造字档案」重组，回到最原始白卡（16:08）
- 用户要求「连私人造字档案重组都没有」= 彻底撤销 15:35 与 15:46 两次字笺升级，回到本会话之前最原始的 P06.5 古籍字卡白卡。
- 三处回退：
  - `index.html` `#page-charcard`：删除 `zhijian` 类与 `.charcard-head/.charcard-body/.charcard-meta/.charcard-sign` 整套笺纸结构，还原为 `.charcard ancient-card` 内 `charcard-header(brand 字造集 + subtitle 我的造字)` → `charcard-char` → `charcard-components` → `charcard-divider` → `charcard-style` → `charcard-meaning` → `charcard-divider` → `charcard-footer`。所有 JS 填充 ID 原样保留。
  - `css/style.css`：删除 `.charcard.zhijian` 整块与所有 zhijian 子规则，还原为原始 `/* ===== 古籍字卡 ===== */` 块（`.charcard` 白底 rgba(255,255,255,0.75)+1.5px 边+6px 圆角；`::before` paper-bg.jpg 纹理 opacity .25；`.charcard-header/.brand/.subtitle/.char/.components/.divider/.style/.meaning/.footer`）。enhance.css 的 `.charcard` 覆盖规则（glass-strong/::before 宣纸/divider 金线）本就是原始时代所写，无需改、自然契合。
  - `js/main.js` `renderCharCard`：删除 `toArchivalNum/structureFallbackName/formatArchiveDate` 三辅助函数与所有字笺字段填充（编号/构形/人格/落款日期），还原为原始逻辑（仅填 char/components/style 显示或隐藏/meaning/userName 落「· 创意生成」）；图片态内联 `width:100px;height:100px`。
- 校验：`node --check js/main.js` SYNTAX_OK；grep 0 处「字笺/私人造字档案/stage/ink/zhijian/meta/sign」残留；8086 返回含 charcard-header+charcard-divider(3 行)、无字笺字样(0)。预览 http://localhost:8086/index.html。

## 开场 6 步叙事（01–06）加 GlowCursor 发光光标（16:14）
- 需求：把 React `<GlowCursor>`（cyc/purple 彗尾光标）效果应用到 zizao.html 的开场 6 步叙事页（非 React，原生实现）。
- 改动（均在 `zizao.html`）：
  - CSS：`.narrative` 背景 `#000` → `#050610`（呼应 GlowCursor wrapper）；新增 `.narv-glow` 画布层（position:absolute; inset:0; z-index:12; pointer-events:none; mix-blend-mode:screen）。
  - HTML：`<canvas class="narv-glow" id="narvGlow">` 置于 narv-glass 之后。
  - JS：新增 `GLOW` IIFE（原生移植，参数对齐 React 版：color #67E8F9 / secondaryColor #A78BFA / trailLength 40 / trailWidth 8 / trailTaper 0.8 / followSpeed 0.16 / glowIntensity 1.9 / glowSpread 1.2 / hotspot 0.65 / brightness 1.25 / pulseSpeed 1.1 / noiseStrength 0.035 / idleFade idleTimeout 700 fadeDuration 900 / blendMode screen）。含彗尾(青→紫渐变+taper)、惯性光晕、热点白核、pulse 呼吸、noise 微抖、静止 700ms 后 900ms 淡出；监听 narrative 的 pointermove/pointerdown 更新目标点；rAF 循环。
  - 接入生命周期：`playNarrative()` 加 `GLOW.start()`；`revealGallery()` 加 `GLOW.stop()`；`resizeNarrative()` 画布列表补上 narvGlow；`narvGlow` 提升为外层 const。
- 校验：`node --check` 全部脚本 SYNTAX_OK；8090 服务返回含 narv-glow/GLOW/#050610。预览 http://localhost:8090/zizao.html（进开场→6 步叙事即见青紫发光光标）。

## 撤销 GlowCursor（16:16，上一条的回退）
- 用户说「撤销」= 撤销 16:14 的 GlowCursor 效果，开场 6 步叙事回到加效果前原始状态。
- 回退（`zizao.html`）：`.narrative` 背景 `#050610`→`#000`；删 `.narv-glow` CSS 与 `<canvas id="narvGlow">`；删外层 `narvGlow` const 与整段 `GLOW` IIFE（~117 行）；`resizeNarrative()` 画布列表去掉 narvGlow；`playNarrative()` 去 `GLOW.start()`、`revealGallery()` 去 `GLOW.stop()`。
- 校验：grep `GLOW|narv-glow|narvGlow|#050610` = 0；`node --check` 全部脚本 SYNTAX_OK。

## 开场叙事新增「00 夜晚 · 一盏灯下」起兴步（16:29）
- 需求：在开场 6 步叙事（01–06）**之前**再加一步，文案「夜晚 · 一盏灯下」，作为场景起兴（灯尚未亮）。
- 实现（`zizao.html` `playNarrative()`）：在 `narrative.classList.add('show')` 之后、原 01 步 `narvLamp.classList.add('on')` 之前，插入「00」步：`await setText('夜晚 · 一盏灯下', { serif:true, gold:true })` → `await wait(2800)` → `clearText()` → `await wait(700)`；随后原 01 步亮灯，形成「夜→灯亮」过渡。
- 顺带更新脚本注释 `6-step` → `7-step`。无屏上步骤计数器，无需改 UI。
- 校验：`node --check` 全部脚本 SYNTAX_OK；8090 服务返回含「夜晚 · 一盏灯下」。预览 http://localhost:8090/zizao.html。

## 开场 7 步叙事「图案动效」亲和力优化（16:37）
- 需求：原叙事偏文字驱动、缺乏亲和力，要求从图案/动效设计角度增强温度与代入感（非加文字）。
- 纯动效增强（均在 `zizao.html`，克制暖色≤10%，符合古风极简）：
  1. **暖尘粒子场（narv-motes）**：新增 canvas #narvMotes + `startMotes/stopMotes/buildMotes/motesStep`（screen 混合、暖琥珀径向渐变、缓慢上浮 + 正弦摇摆 + 呼吸明灭；密度随视口面积 30–72 粒）。`resizeNarrative()` 画布列表补 narvMotes、`motesRAF` 活跃时重建；`playNarrative` 起始 `startMotes()`、`revealGallery()`（含跳过）`stopMotes()`。
  2. **夜月（narv-moon）**：#narvMoon 冷白径向光晕（左上角），00 步 `add('on')` 淡入、01 步亮灯前 `remove('on')` 淡出 → 「夜→灯亮」转场更有画面。
  3. **手机母题（narv-phone）**：#narvPhone 柔光手机轮廓（底部 home 指示），02 步「你打开手机」出现 `add('on')`、本节结束 `remove('on')` → 给文字一个可共情的图象锚点。
  4. **活体灯火**：`lampBreathe` 由仅 brightness 改为 box-shadow 同步呼吸（0→30px/13px 光晕涨落），灯更有生命感。
  5. **更柔的文字入场**：`.narv-text .ch` 起始 blur 10 / translateY 10 / scale 1.06、`inkIn` 1.3s，并加 `text-shadow: 0 0 18px rgba(255,206,150,.22)` 暖光晕，落定后仍留微暖。
- 校验：`node --check` 全部脚本 SYNTAX_OK；grep 新标识 33 处、服务返回含 narv-motes/moon/phone/startMotes。预览 http://localhost:8090/zizao.html。

## 造字能力值卡片（P03 #page-ability）图案动效重构为三角雷达图（16:45）
- 需求：原卡片是 3 条横条进度条（构形/联想/结构）+ 数字，偏直白缺创意；要求从图案动效设计角度优化，更有创意。
- 重构（古风极简、克制暖色）：把横条换成 **SVG 三角雷达图**，三轴=构形能力(顶)/意义联想(右下)/结构意识(左下)。
  - 同心三角网格(34/67/100%) + 三轴线（淡墨）；数据多边形 `radar-poly` 金线描边 + 半透明金填充，`drop-shadow` 呼吸光晕（`radarBreathe` 3.4s）。
  - 入场：多边形从中心 `easeOutCubic` 生长（rAF 0→分数）约 1.05s；顶点朱红墨点随多边形移动到数据位；顶点数值文字（`ability-*-val`，animateScore 计数）同步定位。
  - 中央下方新增 **综合评级 印章**（朱红方印 `grade-seal` 旋转盖下 stamp 动画 + 「综合评级 · 神品/妙品/能品/初品」按均值分级）。
  - 卡片整体 `radarIn` 轻微缩放入场。
- 实现：
  - `index.html`：删 3 个 `.ability-item`（含 ability-fill/value），改 `#abilityRadarWrap` + `.ability-grade`（含 `ability-grade-val`）；保留 value id 供 animateScore。
  - `css/enhance.css`：新增雷达全套样式（ring/axis/poly/dot/val/axisname/grade/seal + keyframes radarIn/radarBreathe/gradeStamp）。
  - `js/main.js`：新增 `RADAR` 常量 + `buildRadar()`(一次性建 SVG：网格/轴线/多边形/顶点组/轴名) + `animateRadar(scores)`(rAF 生长)；`ability()` 去掉原 `$('#ability-*').style.width` 横条逻辑，改为 `buildRadar(); animateRadar(finalScores);` 并算评级写入 `ability-grade-val`；`AppState.finalScores` 仍保留供认证页。
- 校验：`node --check js/main.js` SYNTAX_OK；grep 残留 bar 引用 0、radar 引用 js6/index1/enhance3；8092 服务返回含 abilityRadarWrap/ability-grade-val。预览 http://localhost:8092/index.html（六书实验室→…→进能力值页）。

## 能力值卡片：删综合评级印章 + 三轴「轴名↔分数」等距（17:30）
- 需求：①删除综合评级印章；②构形能力/意义联想/结构意识 三个轴名 与各自分数 的间距相同。
- 删除：
  - `index.html` 删 `.ability-grade` 整块（含 `grade-seal` 朱印 + `ability-grade-val`）。
  - `js/main.js` `ability()` 删评级计算（avg/grade/gv 写入）。
  - `css/enhance.css` 删 `.ability-grade/.grade-seal/.grade-text` 与 `@keyframes gradeStamp`。
- 等距实现：`buildRadar()` 重构——轴名固定在半径 `LABEL_R=R+15` 处；分数文字固定在轴名正下方 `NAME_GAP=15`（viewBox 单位）同 x/同 anchor，三轴完全一致；`animateRadar()` 的 rAF `frame()` 不再更新分数文字坐标（仅墨点沿轴生长 + 多边形 points），分数改由 `animateScore` 在原位计数。去除原每轴不同的 dx/dy 偏移（0,16 / 13,4 / -13,4）导致的间距不均。
- 校验：`node --check js/main.js` SYNTAX_OK；grep ability-grade/grade-seal/综合评级/神品 全项目 0；8092 返回 seal 标记 0、radar-wrap 1。预览 http://localhost:8092/index.html。

## 情绪 BGM 切换：喜悦/安心/期待/振奋 → xiyue.mp3（17:36）
- 需求：选择 喜悦(xiyue)/安心(anxin)/期待(qidai)/振奋(zhenfen) 情绪卡片后，背景音乐切换为 `assets/audio/bgm/xiyue.mp3`（其余 4 种情绪沿用原 home-guqin）。
- 实现（`js/main.js` 内 `AudioEngine` IIFE）：
  - `tracks` 新增 `bgmJoy: 'bgm/xiyue.mp3'`（注意用户给的路径无扩展名，实际文件为 xiyue.mp3）。
  - 新增 `joyEmotions = ['xiyue','anxin','qidai','zhenfen']` 与 `emotionBgmKey()`：读 `AppState.emotion`（已由 init 从 `sessionStorage.zizaoji_emotion` 载入）判断返回 `'bgmJoy'` 或 `'bgmHome'`。
  - `pageBgm(pageId)` 改为 `const key = key0 ? emotionBgmKey() : 'bgmHome'`（保留 null 停播逻辑）。`emotionBgmKey` 同样供 `setMeaningStyle` 复用（原强制 bgmHome，会覆盖意义页 BGM）。
  - 预加载列表补 `'bgmJoy'`，避免切换时未加载导致 play() 失败。
- 机制：`AppState.emotion` 在开场页选卡时写入 sessionStorage，主程序 init 读入；首屏 navigateTo('lab') 调 pageBgm 即按情绪定轨，`startBgm` 自带换轨淡出（old→new），手机端 ensureBgmRunning 恢复播放。
- 校验：`node --check js/main.js` SYNTAX_OK；grep bgmJoy/joyEmotions/emotionBgmKey 共 8 处；8092 返回 main.js 含该逻辑、xiyue.mp3 HTTP 200。预览 http://localhost:8092/index.html（开场选喜悦类卡→进六书实验室应听到 xiyue）。

## 造字结果卡片 P06.5（#page-charcard）图案动效优化（17:58）
- 需求：卡片此前已回退为原始「古籍白卡」（品牌/字/构件/风格/释义/页脚 + 朱印 + 按钮），现要求从图案动效设计角度再加创意（不重构内容/版式，保持古风极简、暖金≤10%）。
- 动效编排（全部 CSS，进页即播、隐藏再进可重播；navigateTo 先 display:flex 后调 charcard()→renderCharCard，时序可靠）：
  1. **墨字落定 ccInk**：`.charcard-char` 自上而下 clip-path 擦入 + blur 12→0 + 轻微 scale，似墨浸入纸（1.0s）。
  2. **印章盖下 ccSeal**：`.forge-seal` 延迟 .42s 由 scale1.5 旋转落定至 rotate(-3deg) scale1（带回弹 cubic-bezier），朱印"啪"地盖上。
  3. **金线横生 ccLine**：两条 `.charcard-divider` 改为 `cc-div1/cc-div2`，scaleX 0→1 从中心展开，错峰 .55/.76s。
  4. **逐条浮现 ccRise**：header/components/style/footer 依次 translateY+blur 上浮（.15/.50/.62/.82s）；按钮 .92s。
  5. **释义逐字浮现 ccCharIn**：`renderCharCard` 把释义拆成 `span.cc-inkch`（DOM 拼接，免注入），按 `0.62 + i*0.035s` 错峰 blur 晕开。
  6. **暖光呼吸 ccGlow**：`.charcard::after` 极淡暖橙径向光晕（中心 50%/36% 落在字后方）4.6s 呼吸，z-index:1 在纸纹之上、文字之下，添纵深。
- 文件：`index.html`（两 divider 加 cc-div1/cc-div2）、`css/enhance.css`（新增 #page-charcard 动效段 + 6 个 @keyframes）、`js/main.js` `renderCharCard` 释义改 per-char span。
- 校验：`node --check js/main.js` SYNTAX_OK；grep enhance.css 动效标识 19、main.js cc-inkch 2、index.html cc-div 2；8092 三文件均返回新代码。预览 http://localhost:8092/index.html（六书实验室→…→造字结果卡片页）。

## 开场画廊·悲伤情绪卡片 细雨滴下落动效（18:11）
- 需求：进入「悲伤」情绪卡片后，添加细雨滴下落动效（细、长、透明，整体极淡，古风克制）。
- 位置：开场画廊 `zizao.html` 的 3D 漂浮画廊——点击卡片 → `enterFocus()` 显示 `detailOverlay`（即"进入情绪卡片"）。雨仅在该卡片为悲伤（item.file==='benshang.png'）时启用。
- 实现：
  - HTML：`#detailOverlay` 内新增 `<canvas id="rainCanvas">`。
  - CSS：`.rain-canvas`（absolute, inset:0, z-index:1, opacity:0→.on 时 1，1.2s 过渡）；`.detail-panel` 加 `z-index:2` 确保文字在雨之上。
  - JS：`RainFX` IIFE（canvas2d，requestAnimationFrame）。drop：细线 len14~40、width0.8~1.4、淡蓝灰 rgba(176,198,224,0.05~0.15)、speed4~9、轻微风斜 slant~1.2~2.2；桌面 150 滴 / 移动 70 滴；y 越界从顶部重生。`start()` 仅在 enterFocus 命中 benshang 时调；`exitFocus()`、`startScrollTransition()` 均 `RainFX.stop()`；`window.resize` 调 `RainFX.resize()`。
- 校验：`node --check` 全部脚本 SYNTAX_OK；grep rainCanvas/RainFX/rain-canvas 共 14 处；8090 返回含 rain 标记 12。预览 http://localhost:8090/zizao.html（画廊点「悲伤」卡→详情见细雨）。

## 悲伤细雨延伸至主程序（18:17）
- 需求（续上条）：雨不止画廊详情，要覆盖整段悲伤体验——进主程序后背景也落雨。
- 实现（主程序 `index.html`+`css/emotion-fx.css`+`js/main.js`）：
  - JS：`AppRain` IIFE（canvas2d + rAF，参数同画廊细雨：细线 len14~40/width0.8~1.4/rgba(176,198,224,0.05~0.15)/speed4~9/风斜，桌面150·移动70 滴，越界顶部重生）。
  - 画布 `appRain` 由 JS 创建，挂到当前页面的 `.emotion-fx` 内（z-index:-1，页面背景图之上、内容之下）→ 真正"背景雨"，不压文字/按钮。
  - `syncAppRain()`：仅当 `AppState.emotion`(去 .png) === 'benshang' 时 `AppRain.start(fx)`，否则 `AppRain.stop()`（移除画布）。切换页面时由 `applyEmotionBackground()`（每次 navigateTo 调用）重新挂到新页面 fx 层，单画布持续、不重建。
  - 新增 `window.resize → AppRain.resize()` 自适应尺寸。
  - CSS：`css/emotion-fx.css` 末追加 `.app-rain`（absolute inset:0, opacity:0→.on 1.2s, z-index:0, pointer-events:none）。
- 校验：`node --check js/main.js` SYNTAX_OK；grep appRain/AppRain/syncAppRain/.app-rain 共 28 处；8092 返回 main.js 25、emotion-fx.css 2。预览 http://localhost:8092/index.html（开场选「悲伤」→进六书实验室即见背景细雨，全程覆盖各页）。

## 删除夜月 + 撤销"悲伤细雨延至主程序"（18:29）
- 需求：删除开场叙事左上角冷白月晕 `narv-moon`；撤销上一步（即"悲伤细雨覆盖整段主程序体验" = AppRain）。
- 删除 `narv-moon`（zizao.html）：CSS `.narv-moon`/`.narv-moon.on`、HTML `<div class="narv-moon" id="narvMoon">`、const `narvMoon`、`playNarrative` 的 `narvMoon.classList.add('on')`(00 步)/`remove('on')`(01 步)、`revealGallery` 的 `remove('on')`；暖层注释改为"living motes + phone motif"。保留 motes/phone/lamp/暖文字。
- 撤销 AppRain（主程序）：`js/main.js` 移除 `AppRain` IIFE + `syncAppRain()` 函数 + `applyEmotionBackground` 内 `syncAppRain()` 调用 + `window.resize→AppRain.resize()`；`css/emotion-fx.css` 移除 `.app-rain` 整段（含注释）。**画廊详情页细雨 RainFX（zizao.html）保留**，仅回退"上一步"。
- 校验：grep narv-moon/narvMoon=0、appRain/AppRain/syncAppRain=0、.app-rain=0；RainFX 仍在 zizao.html(12 处)；`node --check` zizao.html 全脚本 OK、main.js SYNTAX_OK；8090/8092 均 200 且不再含上述标记。预览 http://localhost:8090/zizao.html、http://localhost:8092/index.html。

## 开场 7步叙事提速（19:33）
- 需求：开场 7 步叙事整体快一点。
- 改动（zizao.html `playNarrative` 固定等待，约 -35~40%，保留文字可读性下限）：
  - 00 夜：2800→1700、700→450
  - 01 灯：1600→1000、2400→1500
  - 02 手机：2200→1400、tremble 1500→1000（waitFirstTouch 交互等待不变）
  - 03 无字：1500→1000、1100→700、dissolve 2000→1200、1300→850、900→550
  - 04 无名：3500→2200、800→500
  - 05 古人：2100→1300（enableWipe 交互等待不变）
  - 06 你字：无固定等待（gatherWord 交互）
- 校验：`node --check` zizao.html 全脚本 OK；8090 返回 200 且含新计时。预览 http://localhost:8090/zizao.html。

## 「准确表达」墨散动画保留原速（19:39）
- 需求：开场提速后，step 03「没有任何一个字能够准确表达」的墨散（被水冲散）动画不要加速。
- 改动：仅 step 03 的 `playNarrative` 固定等待复原为提速前原值（其余 6 步保持提速）：1500 / 1100 / dissolve 2000 / 1300 / 900（提速时曾为 1000 / 700 / 1200 / 850 / 550）。
- 说明：`inkWash 2.4s` 视觉动画本身（zizao.html:465）提速时未改，此处仅把墨散停留的 dwell 还回原速，让墨迹有完整时间化开。预览 http://localhost:8090/zizao.html。

## 主程序：5情绪专属画面动效（19:44）
- 需求：除喜悦/愤怒/振奋外，点击情绪卡片后所有页面都要有不同且贴合情绪的画面动效。
- 现状：`applyEmotionBackground()` 已在每次 navigateTo 给当前页注入 `.emotion-fx fx-{emo}` 氛围层（z-index:-1，内容之下）+ `buildEmotionParticles` 注入粒子；原8情绪都有 CSS 效果，但5情绪（安心/悲伤/孤独/恐惧/期待）偏静态弱、缺明显运动。
- 改动：
  - `css/emotion-fx.css`：新增 DOM 粒子样式 `.anxin-mote`(安睡光尘上升) / `.qidai-spark`(上升萤火) / `.gudu-moon`+`.gudu-star`+`.gudu-vignette`(孤月·疏星·隔离暗角) / `.kongju-wisp`(边缘冷雾)；并补入 `prefers-reduced-motion` 名单。
  - `js/main.js` `buildEmotionParticles`：anxin 注入光尘、qidai 注入萤火、gudu 注入孤月+疏星+暗角、kongju 注入冷雾；清理选择器同步新增类名。悲伤细雨/墨晕沿用既有 CSS。
  - 喜悦/愤怒/振奋维持原有效果（花瓣/飞鸟/红雾+震），未动。
- 校验：`node --check` main.js OK；8092 返回 200。预览：本地起 zizao.html 选对应情绪卡片进入主程序（http://localhost:8092/index.html）即可见。
