# 2026-08-31 工作日志

## 胶片画廊（Reel Gallery）— P10 我的字造集
- **需求**：将「我的字造集」页面（P10, `#page-collection`）从 2 列网格改为胶片画廊（film reel）效果
- **参考**：React Bits Pro Reel Gallery 组件的视觉效果（倾斜胶片条、齿孔、帧编号 KODAK PORTRA）
- **实现方式**：原生 HTML/CSS/JS（非 React，遵循项目无构建约定）
- **修改文件**：
  - `index.html`：`collection-grid` → `reel-gallery` 容器
  - `css/enhance.css`：完整重写 section 12，新增 `.reel-gallery/.reel/.reel-track/.reel-frame` 等样式（齿孔伪元素、倾斜层次、深色电影背景、金色光尘）
  - `js/main.js`：`renderCollection()` 改为分配卡片到 3-5 条胶片（交错+首尾重复实现循环感）；新增 `initReelDrag()` 实现指针拖拽+惯性滚动+视差联动+空闲漂移；点击帧→详情弹窗保留
- **验证**：`node --check main.js` exit 0；无残留 collection-grid/collection-card JS 引用

## 撤销胶片画廊（10:10）
- 用户要求撤销胶片画廊效果，恢复原始 2 列网格
- 已还原三处：
  - `index.html`：`reel-gallery` → `collection-grid`
  - `css/enhance.css`：section 12 恢复为 `.collection-grid/.collection-card` 网格样式（删除全部 reel-*）
  - `js/main.js`：`renderCollection()` 恢复为原始网格渲染；删除 `initReelDrag()`；`initCollection()` 移除 `_cleanupReel` 清理逻辑
- **验证**：`node --check main.js` OK；grep 确认无 reel-* / initReelDrag / _cleanupReel 残留引用
- 结论：P10 我的字造集已回到改前状态（2 列卡片网格 + 点击→详情弹窗）

## 情绪驱动背景随机切换（10:27）
- **需求**：开场页点击情绪卡片 → 跳转到 index.html 后，所有后续子页面背景根据用户所选情绪卡片随机切换
- **素材**：`assets/images/` 下 7 张情绪专属图（anxin/fennu/gudu/kongju/qidai/xiyue/zhenfen + bg.png）+ 6 张通用水墨意境图（Background_BambooShadow/CloudPattern/InkWash/Landscape/RicePaper/Seal.png, paper-bg.png）。**benshang 无专属图**，用通用水墨兜底
- **实现**：
  - `zizao.html` `startScrollTransition()`：跳转前 `sessionStorage.zizaoji_emotion = item.file`（如 'anxin.png'）
  - `main.js` `AppState.emotion` 在 `init()` 读取；新增 `getEmotionBgPool()`（情绪专属图 unshift 进候选池 + 通用水墨）+ `applyRandomBackground()`（随机抽图，叠 rgba(247,243,234,0.55) 宣纸蒙版保证可读，设到当前 `.page` 内联背景）；在 `navigateTo` 末调用 → 每翻一页随机切换，且避免与上一张重复
- **验证**：`node --check main.js` OK；grep 确认接线（zizao.html 写 / main.js 读用）齐全
- **注意**：`.page` 原 `background:var(--color-paper)` 不透明，故改设在 `.page` 内联 backgroundImage + backgroundColor transparent，叠宣纸蒙版；海报页等若需保留自身背景后续可在其 pageInit 内重置

## 情绪背景改为固定（10:44）
- **用户撤销**「每翻一页随机换一张」，改为：选了哪个情绪卡片，跳转后**所有页面统一用该情绪专属背景图**（非随机）。例：点「喜悦」→ 全程 xiyuebg.png
- **关键修复**：卡片图 `benshang.png` 对应背景文件实为 `beishangbg.png`（ben↔bei 拼写差异）。新增 `EMOTION_BG_MAP` 显式映射 8 情绪→专属 bg 图；目录已补齐 `beishangbg.png`（此前缺失）。若无情绪则兜底 `paper-bg.png`
- **实现替换**：删除 `getEmotionBgPool/_lastEmotionBg/applyRandomBackground`，改 `getEmotionBg()`（直接返回映射图）+ `applyEmotionBackground()`（固定设当前页背景，仍叠 0.55 宣纸蒙版）；`navigateTo` 末调用 `applyEmotionBackground()`
- **验证**：`node --check main.js` OK；grep 确认无 applyRandomBackground/getEmotionBgPool/_lastEmotionBg/EMOTION_BG_EXISTS 残留

## 情绪会呼吸 + 情绪温度（14:43）
- **需求**：① 8 张情绪背景不是静态图，随停留产生微动（安心·水面荡漾/孤独·云移枝晃/恐惧·雾闪/悲伤·雨丝/愤怒·红烟翻涌/期待·日出/喜悦·花瓣飘/振奋·飞鸟升）；② 整 UI 极淡情绪染色（5~10% 环境色，保持古风极简），各带微动效（喜悦·光晕扩散/恐惧·呼吸暗角/安心·水波/孤独·缓慢远离/悲伤·雨滴/愤怒·微震/期待·光线升/振奋·粒子升）
- **实现**：
  - 新建 `css/emotion-fx.css`：`.emotion-fx` 覆盖层 `position:absolute; inset:0; z-index:-1`（落在 .page 背景图之上、流内容之下，**不污染布局/文字**）；`fx-<emo>` 类携带温度染色（background-color 0.07~0.11 alpha）+ ::before/::after 呼吸动效；花瓣/飞鸟用 JS 注入少量 DOM（`.petal`/`.bird`）+ keyframes
  - `index.html` head 在 enhance.css 后加 `<link ... emotion-fx.css>`
  - `main.js` `applyEmotionBackground()` 改为：设背景后确保 `.page` 首个子为 `.emotion-fx`（`:scope > .emotion-fx`，无则 insertBefore firstChild），置 `fx-<emo>` 类；`fx.dataset.built` 守卫下调用 `buildEmotionParticles()` 注入花瓣(喜悦,9)/飞鸟(振奋,7)，随机 left/drift/duration/delay；宣纸蒙版由 0.55 降至 0.5 让氛围更透出
  - 含 `prefers-reduced-motion` 降级
- **验证**：`node --check main.js` OK；grep 确认 applyEmotionBackground/buildEmotionParticles/insertBefore/fx- 接线齐全
- **设计要点**：用 z-index:-1 而非给内容加 z-index，避免破坏页面内绝对定位装饰；染色仅作用于负空间，文字与控件可读性不受影响

## 造字人格页面套用 ProfileCard 倾斜卡片（React Bits Pro 原生等效）—— 已撤销
- 该改动后续已被用户要求**撤销**：P07 造字人格卡片恢复原样（`.certify-personality` 普通结构），删除全部 `.profile-card*` 样式与 `initProfileCardTilt()`。此处仅留作历史记录，不代表当前代码状态。

## 撤销 ProfileCard 改动 + 新增玄玄 7 场景（来自 docx「帮我加入玄玄出场的网页场景」）
- **撤销**：用户要求撤销 P07 造字人格页面的 ProfileCard 改动。已还原 index.html P07 结构、style.css `.certify-personality`、enhance.css 全套 `.profile-card*`、main.js 的 handle/initProfileCardTilt 调用。node --check OK，无残留引用。
- **玄玄素材**（`assets/xuanxuan/`）：tantou(探头)/zhayan(眨眼)/tuosaisikao(托腮思考)/xiaoweibabianchengwenhao(尾巴问号)/bianchengmu(变木)/bianchenghuo(变火)/xiaoshujuanjilu(小书卷)/tiaoqilai(跳起，已去空格改名)/xueshimao(雪狮猫博士帽)/xuanxuan(本体)。
- **实现**：`js/components/XuanXuan/xuanxuan.js` + `xuanxuan.css` 已存在基础 stub（navigateTo 里 `XuanXuan.show(pageId,line)` 调用）。增强为完整 7 场景角色系统：
  - 跨文件 base 路径：`setBase()` 适配 zizao.html（长前缀 `zizaoji4_final_xuanxuan_audio_button/zizaoji4_final_xuanxuan_audio_button/`）与 index.html（空）。
  - POSES/PAGE_POSE/PAGE_LINE 映射；`show(page,text)` 按页设姿态+定位+台词；`_greetZizao()` 墨池探头→眨眼→抖墨点(setTimeout 三段台词)；`transform(id)` 木(tree)/火(fire) 变形+台词；`comfort()` 组合失败安慰；`jumpForJoy()` 字卡跳起；`xuan-enter`/`xuan-jump`/`xuan-ink-dot`/`xuan-bubble-in` 动效；`prefers-reduced-motion` 降级。
  - 场景映射：①情绪字卡页(zizao.html·revealGallery)→探头三段台词；②工坊(workshop)→尾巴问号+「你的此刻，会诞生怎样的文字呢？」；③点木/火(addComponent)→变形+台词；④btn-generate 单构件→comfort「咦？它还在等待另一半呢。」；⑤分析/释义(analysis/meaning)→小书卷「让我把这个字的故事记下来。」；⑥字卡(charcard)→跳起「我把它记下来了！」；⑦字造集(collection)→雪狮猫「每一个字，都有属于自己的故事哦」。
- **改动文件**：xuanxuan.js(重写)、xuanxuan.css(重写+保留各页定位)、main.js(navigateTo 台词对齐+addComponent transform+btn-generate comfort)、zizao.html(head 引入 css/js+setBase；revealGallery 调 show('zizao'))。
- **验证**：`node --check main.js` & `node --check xuanxuan.js` 均 OK；grep 确认 show/transform/comfort/setBase/show('zizao') 接线齐全；components.json 中木 id=`tree`、火 id=`fire` 与 transform 匹配。

## 玄玄位置调整（18:47）—— 不再遮挡正文/按钮
- **问题**：`#xuanxuan` 为 `z-index:9999` + `pointer-events:none`（不拦点击，但会视觉遮挡）。原定位有重叠：工坊 `left:20px;top:200px` 压到中央造字画布；实验室 `right:60px;top:180px` 压到演变动画/时间轴；多个页面右下角压到底部按钮。
- **调整**（`js/components/XuanXuan/xuanxuan.css`）：
  - 角色尺寸 88→80px（移动端 64→60px），缩小视觉占地。
  - 全部收进右下角并抬到按钮之上：lab `right:16;bottom:24`、workshop `right:16;bottom:78`、analysis/meaning `right:16;bottom:84`、charcard `right:16;bottom:96`、collection `right:16;bottom:18`(仅压免责声明一行)；zizao 仍底部中央墨池探头不动。
  - 气泡统一在角色上方（始终在屏内、不压正文）；lab/workshop 气泡改为右对齐(`right:0`)，不再指向左。
  - 移动端 media 同步更新右下角坐标。
- **验证**：grep 确认仅 xuanxuan.css 定义这些类，无其它样式表冲突。

## 玄玄位置二次精调（20:12）—— 基于用户截图逐页修复
- **用户反馈**（附 6 张截图）：
  - 工坊页：玄玄压在右侧构件库（日/月/山/水/火）上，气泡盖住结构标签
  - 字卡页：气泡「我把它记下来了！」明显压住「查看造字资格」按钮（红框标注）
  - 分析/释义页：气泡压到底部文字「构形完成，正在赋予意义…」
  - zizao 开场页：气泡与「开始造字之旅」按钮重叠
  - 海报页三个按钮（我的字造集 / 保存海报 / 重新造一个字）需要缩小
- **本次修改**：
  1. **气泡全局缩小**：max-width 184→160px，padding 8px 12→6px 10px，font-size 12→11px，border-radius 16→14px，阴影减弱；移动端进一步缩至 max-width 130px/font 10px
  2. **工坊** `xuan-workshop`：从右侧 `right:16;bottom:78` → 左侧 `left:10;bottom:80`（右侧是构件库密集区，左侧是空白）；气泡左对齐
  3. **字卡** `xuan-charcard`：bottom 从 96→**130px**（大幅抬高，远高于按钮）
  4. **分析/释义** `xuan-analysis/.xuan-meaning`：bottom 从 84→**110px**
  5. **zizao** `xuan-zizao`：bottom 从 14→**6px**（角色更贴近底边，给上方气泡留更多屏内空间）
  6. **海报按钮缩小**（style.css）：`#page-poster .poster-actions .btn-secondary/.btn-generate` padding 8px 14px/font 13px/border-radius 20px；`.btn-new-char` padding 10px 20px/font 13px
  7. 移动端断点同步更新全部坐标；角色尺寸 80→**56px**（移动端）
- **改动文件**：`js/components/XuanXuan/xuanxuan.css`（定位+气泡尺寸）、`css/style.css`（海报按钮缩小）

## 玄玄位置三次精调（20:57）—— 工坊+zizao 彻底避让
- **用户反馈**（附 3 张截图，红框标注）：
  - **工坊页**（最严重）：玄玄在左下角 `left:10;bottom:80` 依然压住构件库（日/月/山/水/火/木）和「自然」标签；气泡飘到左上压画布区 → **工坊四周全是内容**（画布居中/结构标签+构件库占满底部），左下不是安全区
  - **zizao 开场页**：底部中央的气泡与「开始造字之旅」按钮仍有重叠
- **本次修改**（`xuanxuan.css`）：
  1. **工坊 `xuan-workshop`**：从左下彻底移到**右上角装饰空白区** `right:8px;top:72px`（标题下方、树枝装饰处，唯一无内容区域）
  2. **工坊气泡方向翻转**：默认气泡在角色上方(`bottom:106%`) → 改为在角色**左侧**(`right:106%;top:50%;translateY(-50%)`)，气泡箭头朝右指向角色，文字向左展开不压任何内容；`::after` 伪元素改为左向三角(`border-left-color`)
  3. **zizao `xuan-zizao`**：从底部中央 `left:50%` → 移到**右下角** `right:20px;bottom:6px`（避开居中按钮）；气泡同样改为左侧展开(`right:106%`) + 左向三角
  4. 移动端断点同步：workshop `right:6;top:60`、zizao `right:12;bottom:4`、气泡 `right:104%`
- **改动文件**：仅 `js/components/XuanXuan/xuanxuan.css`

## 玄玄位置四次精调（21:05）—— 气泡贴边+zizao 删除
- **用户反馈**（附 5 张截图）：
  - #1 工坊：气泡「今天，你想创造…」**太靠下**（几乎贴底）
  - #2 zizao 开场：**删除玄玄**
  - #3 分析页：气泡**太靠右**（贴边），整体**往下调**
  - #4 字卡页：气泡**太靠右**，整体**往下调**
  - #5 字造集：气泡**太靠右**
- **根因分析**：lab 的 `.xuan-dialog` 被覆盖为 `right:0;transform:none`（右对齐→贴边）；analysis/charcard/collection 虽用默认居中气泡，但角色 `right:12px` 太贴边导致气泡视觉上偏右
- **本次修改**（`xuanxuan.css`）：
  1. **zizao**：`.xuan-zizao{display:none!important}` — 彻底隐藏玄玄
  2. **工坊气泡**：从 `top:50%;translateY(-50%)`（垂直居中于角色→偏下）改为 `top:-8px`（气泡顶部高于角色顶部），不再贴底
  3. **实验室气泡**：从 `right:0;transform:none`（右对齐贴边）改回 `left:50%;translateX(-50%)`（居中于角色上方）
  4. **analysis/meaning**：角色 `right:12→24px`（内移 12px 离开边缘）；`bottom:110→70px`（下沉）
  5. **charcard**：角色 `right:12→24px`；`bottom:130→90px`（下沉）
  6. **collection**：角色 `right:12→24px`；`bottom:14→10px`
  7. 以上三页不再覆盖 dialog 样式 → 全部使用默认**居中气泡**（`left:50%;translateX(-50%)`），不再偏右
  8. 移动端同步更新全部坐标

## 撤销 21:05 改动（21:18）
- 用户要求「撤销上一步」。已将 `xuanxuan.css` 恢复到 20:57 版本（第三次精调）状态：
  - **恢复 zizao 玄玄**（撤销 `display:none!important`，改回 `right:20px;bottom:6px` 右下角+左向气泡）
  - **恢复工坊气泡**：`top:-8px`（高于角色）撤销，改回 `top:50%;translateY(-50%)`（垂直居中于角色左侧）
  - **恢复实验室气泡**：`left:50%` 居中撤销，改回 `right:0;transform:none`（右对齐）
  - **恢复 analysis/charcard/collection**：`right:24→12px`（外移到贴边）、`bottom` 回调（analysis 70→110、charcard 90→130、collection 10→14），气泡恢复默认位置
  - 移动端坐标同步回退
- 结论：当前 xuanxuan.css == 20:57 版本。如后续要重新处理 zizao 隐藏/气泡贴边问题，需用户再次明确要求。

## 情绪动效增强——落实《5种情绪动效设计方案》（21:26）
- **来源**：用户附 `F:/5种情绪动效设计方案.docx`，要求「增加设计」。文档给出 5 种情绪的签名动效细节。
- **现状对照**：emotion-fx.css 已有 8 情绪呼吸+温度基础，但文档细节缺失。本次按文档补全 5 种：
  - **安心**：新增 `anxin-bamboo` 竹影摇曳（CSS）；`pointerdown` 触摸水波（JS 在触点生成 3 圈 `.anxin-ripple` 扩散消失）——「用户触摸哪里冒水波」
  - **孤独**：新增 `gudu-moon` 固定月 + `gudu-star` 偶尔闪烁（7s 长周期）+ `gudu-mountains` 远山缓慢后退（48s）；云速 26s→40s 更慢（文档：动效非常慢）
  - **悲伤**：雨丝改为「细、长、透明」（98° 更细更稀、opacity .42/.32、1.6s/2.4s）；新增 `benshang-ink` 墨迹晕染（底部上升）
  - **期待**：新增 `qidai-line` 底部极细金线 + `qidai-cloud` 云雾逐渐散开（17s）
  - **恐惧**：新增 `kongju-light` 远处极淡光闪（JS 每 6.2s 在随机边缘生成一次）；新增 `.kongju-jitter` 汉字极短 1px 偏移（160ms 即复位，不一直抖——文档强调「不要一直抖」）
- **机制**：复用 `applyEmotionBackground→buildEmotionParticles(fx,emo)` 在 `.emotion-fx`（z-index:-1，内容之下）注入 DOM；新增 `setupEmotionInteractions()` 一次性绑定全局 `pointerdown` 与 `setInterval`，受 `prefers-reduced-motion` 与 `window.__zizaojiEmotionFx` 守卫。
- **验证**：`node --check main.js` OK；emotion-fx.css 大括号 124/124 平衡。
- **改动文件**：`css/emotion-fx.css`（新增增强层+雨水调细+reduced-motion 扩充）、`js/main.js`（buildEmotionParticles 注入 + setupEmotionInteractions）

## 撤销孤独（gudu）动效增强（21:40）
- **用户要求**：撤销《方案》中 🌙 孤独的「月固定 + 云缓移 + 星偶尔闪 + 远山后退」增强（gudu-moon / gudu-star / gudu-mountains），并云速 40s 回退 26s。
- **回退内容**：
  - `js/main.js`：删除 `buildEmotionParticles` 中 `emo==='gudu'` 注入分支；清理 querySelectorAll 里的 gudu-* 类名
  - `css/emotion-fx.css`：删除 `.fx-gudu .gudu-moon/.gudu-star/.gudu-mountains` 及 `@keyframes starTwinkle/mountainRecede`；云速 `cloudDrift 40s→26s`；reduced-motion 块移除 gudu-* 引用
- **保留**：孤独仍只有原有的「云缓移 + 树枝轻晃」(cloudDrift 26s / branchSway)；其余 4 种情绪（安心/悲伤/期待/恐惧）增强不动。
- **验证**：`node --check main.js` OK；emotion-fx.css 大括号 114/114 平衡；grep 确认 gudu-moon/star/mountains 全仓无残留。

## 造字人格页 · Lanyard（吊牌）原生等效效果（21:43）
- **用户需求**：把 React Bits `Lanyard` 组件效果用到「造字人格/认证页」(page-certify)。该组件是 3D 吊牌：卡片从挂带垂下、受重力摆动、指针视差倾斜、正/背面可换图（frontImage/backImage/lanyardImage 等 props，需 R3F + card.glb + lanyard.png）。
- **约束**：本项目是原生单文件 H5，无 React/Vite/GLB 资源 → 按项目约定用原生 HTML/CSS/JS 实现等效效果（不引入 React Bits 付费/构建依赖）。
- **实现**：
  - `index.html`：用 `.lanyard > .lanyard-swing > .lanyard-tilt` 包裹 `.certify-card`；新增 `.lanyard-strap`(织带)+`.lanyard-clip`(金属扣)；卡片拆为 `.certify-face-front`(原数据绑定内容，id 不变)+`.certify-face-back`(字造集收藏印，含 `#certify-back-name`)。
  - `css/style.css`：新增 lanyard 全部样式——`perspective` + `transform-origin:top center`(吊点) + `@keyframes lanyardSwing`(rotateZ ±2.4° 钟摆式重力摆动) + `--rx/--ry` 指针视差倾斜 + `.certify-card` 3D `rotateY(180deg)` 翻面(`backface-visibility:hidden`)；`.lanyard .certify-card{overflow:visible}` 以保 preserve-3d 翻转；`prefers-reduced-motion` 关闭动画/视差。
  - `js/main.js`：新增 `initLanyardParallax()`（`pointermove`→设 `--rx/--ry`，受 reduced-motion 与页面可见性守卫；`pointerleave` 复位）；`#certify-card` 点击 toggle `flipped`；`renderCertification()` 填 `#certify-back-name` 并重新进入时复位到正面。
- **验证**：`node --check main.js` OK；style.css 大括号 416/416 平衡；grep 确认 certify-name/certify-back-name/lanyard-tilt/certify-card 接线齐全、数据 id 无重复。
- **交互预期**（真机确认）：认证页卡片默认随重力轻摆；移动/倾斜光标时卡片 3D 跟随；点击卡片翻到背面「字造集收藏印」；再次点击翻回。

## 我的字造集 · Reel Gallery 效果（重新实现，22:02）
- **背景**：用户曾于 10:10 撤销过「胶片画廊」(film-strip+sprocket 版)。本次**重新要求**应用 React Bits Pro *Reel Gallery* 的视觉效果，并给出官方文档/组件链接。
- **React Bits Reel Gallery 核心**：倾斜堆叠的多行图片卷轴(tilt)，随滚动/拖拽横向滑动(glide)，含 autoScroll 自动漂移、speedVariance 逐行速度差、alternate 相邻行反向、静止 grayscale 去色 + 光标 focus 聚光恢复彩色、边缘 fade 淡出、外侧卷轴 dim 变暗 + taper 缩小、arch 深度后退。
- **本项目约束**：原生 HTML/CSS/JS（无 React/Vite/React Bits 付费 license），按约定用原生等效实现，不引入 React 依赖。
- **修改文件**：
  - `css/style.css`：① `#page-collection` 改 `display:flex;flex-direction:column`；② 删除原 `.collection-grid` 2列网格规则，新增 `.reel-gallery/.reel-stack/.reel/.reel-plate/.reel-glyph/.reel-name/.reel-tag/.collection-empty-wrap` 全套样式（perspective+rotateX(tilt) 倾斜整叠、mask 边缘淡出、touch-action:none 接管横向拖拽、grayscale 静止去色、.focus 聚光恢复）；③ `.collection-empty` 去除 `grid-column`。
  - `js/main.js`：`renderCollection()` 重写——卡片按行轮流分配(字少则复制铺满每行 minPlatesPerReel=8)，渲染 `#reel-stack>.reel>.reel-plate`；新增 `REEL` 配置对象(对应 React Bits 全部可调参数) + `bindReelHandlers`(wheel/pointer 横向滑动+惯性+点击判定→详情) + `updateReelFocus`(光标聚光) + `startReelGallery/stopReelGallery/reelTick`(rAF 循环，离开页面自动停)。空状态与「继续造一个字」按钮保留。
- **验证**：`node --check main.js` exit 0；grep 确认无旧 `initReelDrag/_cleanupReel` 残留；enhance.css 无遗留 reel-*（此前撤销干净）。
- **预览**：本地 `cors_server.py 8084` 已起，http://localhost:8084/ 进入「我的字造集」(顶栏 集) 查看；首次空库需先造字或 localStorage 注入 `zizaoji_collection` 种子。

## 新建独立 H5：汉字演变高速路（22:40）
- **需求**：用 Hyperspeed 高速路视觉展现「中华汉字演变历史」，需适配 PC + 手机端。用户提供了 react-hyperspeed 组件参数（distortion/length/roadWidth/lanesPerRoad/colors 等）。
- **技术选型**：未引入 React+react-three-fiber 构建链；改用**单文件 HTML + 原生 Three.js(0.160, CDN importmap) + 自定义着色器**复刻高速路效果（更稳定、可双击直接预览、便于移动部署）。
- **文件**：`F:\新建文件夹 (3)\zizao-hyperspeed.html`（自包含单文件）。
- **实现要点**：
  - 路面用 ShaderMaterial：顶点正弦位移做 turbulent 起伏；片元用 UV 画白色车道线（路肩实线 + 车道间虚线），颜色沿用用户配置（road 0x080808、车流 leftCars 品红/右青、sticks 青 0x03b3c3）。
  - 车流 = 发光拉伸 Box（品红/青/蓝），沿 4 车道向相机流动并循环；路侧光柱 = 青色细高 Box 循环。
  - UnrealBloom 泛光；切章节时 `speedBoost` 瞬时提速 + FOV 放大(90→~150)模拟「加速」。
  - 移动端降 pixelRatio(1.5)/减少车与光柱数量/降 bloom；resize 自适应。
- **内容层（覆盖在高速路上）**：以「车」字为线索，6 章演变 甲骨文→金文→小篆→隶书→楷书(車)→简体(车)；甲/金/篆/隶用手绘 SVG，楷/简用字体；卡片 PC 右侧、手机底部抽屉；自动播放(7s)+前后/暂停/圆点导航；墨黑+金+朱砂的中式配色与高速路青品红形成对比。
- **验证**：`node --check` 模块脚本 OK；含 WebGL 不支持兜底提示与「进入高速路」启动按钮（满足手势初始化/字体加载）。
- **注意**：运行时依赖 unpkg CDN（three@0.160），预览需联网。

## 升级：高速路改「弯曲+上下坡」，汉字演变发生在路上（22:47）
- **新需求**：① 高速路要有左右曲折 + 上下坡（像真实道路，相机沿路行驶）；② 汉字变化要随时间在高速路上不断演变（不只是侧栏卡片）。
- **实现**：
  - 路面改为**参数化路径 ribbon**：顶点着色器按沿路参数 p 计算 `pathX(p)`(左右弯) + `pathY(p)`(上下坡) + `yawOf(p)`(横截面旋转对齐切线)；路面车道线/中央隔离带在片元里按 UV 绘制，随几何一起弯曲。相机每帧用 JS 复刻同一 path 函数「骑」在路面上（camera.position=pathX/ pathY+3，lookAt 前方点），形成真正驾驶感。
  - car/光柱/里程碑字形全部用 treadmill 方式：每个物体存参数 p（沿路位置）与横向偏移 ox，世界坐标=`path(p)+旋转(ox)`，随 uScroll 增长而向相机流动并循环（LOOP=480）。
  - **汉字演变上路面**：沿路每 80 单位漂浮一个发光巨字里程碑（Sprite + CanvasTexture，金字形+时期名），依次为 甲骨文→金文→小篆→隶书→楷书(車)→简体(车)；字形从 SVG(甲/金/篆/隶)与字体(楷/简)生成。相机驶过时侧栏卡片自动同步到最近里程碑（idx 跟随），点圆点/上下章则把对应里程碑拉到相机前（uScroll=10-(base+i*spacing)）。
  - 暂停=冻结前进；切章触发 speedBoost(FOV 90→约150)模拟加速。
- **验证**：`node --check` 模块脚本 OK；pathX/Y/yawOf 在 GLSL 与 JS 必须一致（已对齐，单位弧度一致）。

## 撤销 ScrollExpand & 调整情绪画廊提示与玄玄气泡（23:35）
- **撤销**：移除 `zizao.html` 中 `ScrollExpand` 的 CSS/HTML/JS，恢复 `startScrollTransition` 原先黑屏遮罩直接跳转 `index.html` 行为。
- **提示上移**：将六书情绪画廊底部提示“拖动漫游 · 双指缩放 · 点击聚焦”移到 `header` 副标题下方，改为相对定位、更小字号、更低透明度，避免遮挡画廊。
- **气泡上移**：`xuanxuan.css` 中 `.xuan-zizao` 的 `bottom` 从 `6px` 提升到 `120px`（移动端 `86px`），让玄玄对话气泡整体向上移动，不再贴近屏幕底部。
- 仍可优化：弯道可加 banking 侧倾、里程碑可加朝代年份标牌。

## 开场跳转点叠加 ScrollExpand 效果（23:05）
- **需求**：参考 React Bits Pro <ScrollExpand>，在开场「写完字成功后跳转下一页面」前叠加滚动展开效果；用户选「叠加在现有跳转点」——保留 6步叙事→画廊→开始造字之旅全流程，仅把最后的黑屏遮罩跳转换成 ScrollExpand。
- **文件**：`F:\新建文件夹 (3)\zizao.html`（开场单文件 H5 入口）。
- **实现**：
  - HTML 新增 `#scrollExpand` 覆盖层（z9990）：`.se-media`(带框小图) + `.se-img`(kaichangbeijingtu.png) + `.se-overlay`(标题/下滑提示/诗句子内容/进度条)。
  - CSS：起始 `.se-media`=42vw×56vh、圆角18px；随进度插值到 100vw×100vh、圆角0；`.se-img` scale 1.05→1.35(mediaZoom)；标题/提示/子内容/进度条按进度淡入淡出。
  - JS：`startScrollTransition(item)` 改为先记录 bgm 进度+情绪 sessionStorage，再 `showScrollExpand(item)`；引擎用 wheel/touch 拖拽驱动进度(seTarget，lerp 缓动)，展开满屏(seP≥0.999)后 `finishScrollExpand()` 复用现有 `#scrollMask` 黑屏淡入再 `location.href=NEXT_PAGE`。点击兜底=直接展开到满屏（防桌面无滚轮卡死）。
- **验证**：`new Function` 编译两个内联 <script>（主脚本 60KB）均 SYNTAX_OK。
- **预览**：根目录起 `cors_server.py 8085`，http://localhost:8085/zizao.html → 写完字→叙事→画廊→开始造字之旅→ScrollExpand 滚动展开后跳转。
