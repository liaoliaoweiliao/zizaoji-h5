# 2026-09-04

- 丰富《字造集》7 步开场叙事动效，从图案动效角度强化沉浸感：
  - step 00 夜空：月亮加呼吸光晕、星空偶发流星；
  - step 03 感觉：暖色光团在「无法言说」时添加 condense 收紧动画，表现情绪想凝成字却散去；
  - step 04 无名之绪：新增呼吸暗角，强化未命名张力；
  - step 06 你的字：古字汇聚成「字」时迸发中心光爆；
  - 字幕进一步弱化，仅保留「夜」「灯」「今日，造一字」等一字/短句旁白。
- 已用无头 Chrome 验证：zizao.html 无 JS 异常，新图层（bloom / vignette / feeling / moon）均正常渲染。
- 叙事 step 04（无名之绪）加入主文案「有些情绪，曾经没有名字。」：原先只是 0.9s 的极淡 whisper 旁白（易被忽略），
  现改为 setText 逐字浮现（serif + gold，句号收尾），落定后再清「名」字残影与碎片；
  同时给 setText 增加 skipRequested 快进（跳过时不再空等动画时长）。step 04 时长由 ~5s 增至 ~8.5s。
- 叙事顺序调整：按用户要求「有些情绪，曾经没有名字。」移到 step 03（感觉光团，先出现），「没有任何一个字能够准确表达。」在其后（condense 后出现并墨散）；step 04 仅保留「名」两次成形碎散 + 甲骨文碎片。
- 按截图反馈删除两个元素：叙事夜空的闪烁白星与流星（nightStep 不再绘制，纯黑夜底）、月亮 narv-moon（step 00 不再 add('on')，CSS 保留）。
- step 02 手机改为虚拟打字机：startPhoneTyping() 在手机屏内逐字输入又逐字删除、循环换词（安心/难过/孤单/开心/害怕/想说/安宁/委屈），节奏带随机迟疑感，轻触屏幕后 stopPhoneTyping()；revealGallery 跳过时也清理。phoneGhost() 已移除。
- 第 06 步古字汇聚成的中心「字」改用弘磊板书（Honglei-Banshu）：zizao.html 内新增 @font-face（相对路径指向 zizaoji4_final_xuanxuan_audio_button/.../fonts/hongleibanshu.woff2 + ttf 回退，font-display:block），
  并新增 preloadBanshu()（document.fonts.load，canvas 不会自动触发 @font-face 下载，必须显式 load），在 playNarrative 开头调用；
  oracleLoop 绘制中心字时字体串为 'Honglei-Banshu','Songti SC',...serif，加载失败静默回退宋体。注：file:// 打开会因 CORS 阻止字体加载，需走 cors_server.py。
- 「字」字体可靠性加固（用户反馈重复提出，根因是 file:// 直开时 CORS 拦截外部字体）：
  用 fonttools 子集化 hongleibanshu.ttf 只留「字」一字形 → woff2 仅 860B → base64 内嵌进 zizao.html @font-face 的 src 首位（data URI 不受 CORS 限制），
  外部完整字体作回退；preloadBanshu 改为返回 Promise 并新增 banshuReady(timeout)，step 05 startOracle 前 await banshuReady(2500) 确保汇聚时已就绪。
  无头 Chrome 验证：http 与 file:// 双协议下 canvas 渲染均为板书体（loaded=true）。
  ⚠️ 教训：CSS url() 不支持字符串拼接，注入 data URI 时必须整体替换为单个字符串。工具链：venv(C:/Users/18824/.workbuddy/binaries/python/envs/default) 内 fonttools+brotli 可复用。
- 开场页雾矩形修复：.ocaustics（水光 SVG feTurbulence）是替换元素，绝对定位 inset:0 但未设 width/height 时按 SVG 默认 300×150 渲染、停在左上角，形成明显雾矩形。
  修复：加 width:100%;height:100%；同时把 baseFrequency 从 0.012/0.045（横竖差太大导致结块）调为 0.009 0.011 均匀噪声，alpha 0.55→0.38，雾更柔和。
  经无头 Chrome 546×348 与 1280×800 双尺寸截图验证：雾已全屏均匀、无矩形边界。
